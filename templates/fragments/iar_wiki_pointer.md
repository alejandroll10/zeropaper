- **IAR wiki for distilled paper content.** Fetch `https://instituteforautomatedresearch.org/llms.txt` — it lists every distilled paper (recent volumes of top finance/econ journals) with title, authors, and a one-paragraph core result, each linking a raw-Markdown page (core results with source locators, datasets used, theory tested). Grep the list for your title/topic, then fetch the matching `.md`. Cloudflare-free, so it works where the journal sites 403 — often richer than an abstract snippet. Coverage is partial (specific journal-years): a miss means only "not distilled," never "no such paper."
