### Seeded-mode override (applies because `seeded: true` in `pipeline_state.json`)

**Gate 4 REVISE / MAJOR REWORK**: return to Stage 2 keeping the seed's core mechanism intact. Address only the scorer's **correctness/rigor** comments (math gaps, unclear derivations, missing characterizations). Do NOT adopt scorer suggestions that propose a different framing, different mechanism, or different research question — the scorer does not know this is a seeded project.

**Gate 4 ABANDON**: stop and write `output/seed/abandon_report.md` with a post-mortem. Do NOT return to Stage 0 or Stage 1 to pick a different idea.

**Escalation rules**: the "5 theories → Stage 0" and "delta < 3 → escalate one level" rules do NOT apply in seeded mode in a way that swaps the idea. If the scorer plateaus, switch to the extension playbook (deeper math, new characterizations) while preserving the mechanism.
