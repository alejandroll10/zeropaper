### Seeded-mode override (applies because `seeded: true` in `pipeline_state.json`)

**Gate 2 FAIL**: revise the theory **in place** while preserving the seed's mechanism. Do NOT regenerate a fundamentally different theory. After 3 consecutive failures on the same theory, stop and write `output/seed/abandon_report.md` with a post-mortem — do not silently increment `theory_attempt` into a new idea.
