### Seeded-mode override (applies because `seeded: true` in `pipeline_state.json`)

**Gate 1c BLOCKED**: do NOT abandon the idea. Report the blockage to `output/seed/prototype_blockage.md`, then try **one alternative formalization** (different state variable, different equilibrium concept, different time structure — anything that preserves the seed's mechanism). If the alternative is still BLOCKED, stop and append the post-mortem to `output/seed/prototype_blockage.md`; do not swap the idea.
