# Theory-LLM extension Python deps.
# Single source of truth: read by scripts/apply_extension_theory_llm.sh (during
# setup), then installed by setup's provisioning step.
# This extension source file is a build input. Setup also copies it into the
# manifest-owned `.arpipeline/update_inputs/deps/` provisioning record inside
# each deployment.
openai
anthropic
python-dotenv
