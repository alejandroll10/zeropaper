
**Mirror rule for Stage 3b (this run has `--ext theory_llm`).** If the theory revises after the first Stage 3b pass, re-invoke experiment-designer before Gate 4. Save each execution attempt to fresh `output/stage3b/experiment_results_vN_aK.md` report/plan/bundle/receipt/code/artifact/exhibit paths (N = theory version, K = attempt); do not overwrite the original or a failed attempt. Because downstream state has one report/receipt pair, a replacing re-fire must be cumulative: declare prior accepted report/bundle/receipt evidence as inputs, carry every still-used prior result into the fresh bundle/report/exhibits, add or recompute the new experiment, and pass every absorbed receipt through `--supersedes`. A partial targeted bundle may not replace the pointers. Keep the receipt pending through reviewer ACCEPT and the mandatory contradiction check; then activate it, atomically set `stage3b_theory_version`, `stage3b_results_path`, and `stage3b_result_receipt`, and only afterward retire every predecessor with `--superseded-by` the replacement. Gate 4 cannot advance while stale.
