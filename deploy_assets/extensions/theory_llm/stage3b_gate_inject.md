
**Mirror rule for Stage 3b (this run has `--ext theory_llm`).** If the theory revises after the first Stage 3b pass — new propositions, new implications, new comparative statics, or any content not tested experimentally in the prior pass — re-invoke the experiment-designer on the new content before Gate 4 advances. Save targeted re-runs to `output/stage3b/experiment_results_vN.md` (where N is the theory version); do not overwrite the original `experiment_results.md`. Combined coverage must span the version that will be written into the paper. On completion, set `pipeline_state.json:stage3b_theory_version` to the current `theory_version`. Gate 4 must not advance while `stage3b_theory_version < theory_version`. The mandatory puzzle-triage entry check (see `docs/stage_3b_experiments.md`) re-runs on each re-fire.
