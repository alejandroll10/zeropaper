**`stage3b_theory_version`** (this run has `--ext theory_llm`)**:** Set to the `theory_version` that Stage 3b last fully covered. Before advancing at Gate 4, the orchestrator must verify this equals the current `theory_version`; if it is stale, re-run Stage 3b on the new content (see `docs/stage_3b_experiments.md` "Re-fire on theory revision" and `docs/stage_2.md` Stage 2b step 5 mirror rule).

**`stage3b_results_path` / `stage3b_result_receipt`** (this run has `--ext theory_llm`)**:** Exact report and active receipt from the latest experiment-designer attempt that passed receipt verification, experiment-reviewer ACCEPT, and contradiction checking. Every downstream experiment consumer reads these pointers, never a guessed canonical/versioned sibling. After ACCEPT, activate the new receipt, atomically update both pointers with `stage3b_theory_version`, then retire a superseded prior receipt. The overlap makes every crash point leave the stored pointer naming an active receipt; a failed or pending attempt never changes it.
