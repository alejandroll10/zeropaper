
**Mirror rule for Stage 3a (this run has `--ext empirical`).** If the theory revises after the first Stage 3a pass — new propositions, new implications, new comparative statics, or any content not tested empirically in the prior pass — re-invoke the empiricist on the new content before Gate 4 advances. Save targeted re-runs to `output/stage3a/empirical_analysis_vN.md` (where N is the theory version); do not overwrite the original `empirical_analysis.md`. Combined coverage must span the version that will be written into the paper. On completion, set `pipeline_state.json:stage3a_theory_version` to the current `theory_version`. Gate 4 must not advance while `stage3a_theory_version < theory_version`. The mandatory puzzle-triage entry check (see `docs/stage_3a_empirical.md`) re-runs on each re-fire.
