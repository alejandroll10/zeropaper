
**Mirror rule for Stage 3a (this run has `--ext empirical`).** If the theory revises after the first Stage 3a pass — new propositions, new implications, new comparative statics, or any content not tested empirically in the prior pass — re-invoke the empiricist before Gate 4 advances. Follow `docs/stage_3a_empirical.md` "Re-fire on theory revision": allocate fresh `empirical_analysis_vN_aK.md` report/plan/bundle/receipt, code, input, artifact, and exhibit paths; declare the prior accepted report/bundle/receipt as inputs; and produce one cumulative replacement containing every still-valid old result plus the new/recomputed evidence. Pass each absorbed prior receipt through `--supersedes`; a partial targeted bundle may not replace the Stage 3a pointer. Stage 3a step 7.5 is the sole activation/handoff owner: after the replication and audit triad passes, it activates the new receipt, atomically sets `stage3a_theory_version`, `stage3a_analysis_path`, and `stage3a_result_receipt`, then retires each predecessor with `--superseded-by` the new receipt. The subsequent puzzle-triage contradiction check only routes that already-active accepted evidence and must not activate or hand off it again. Gate 4 must not advance while the version or either pointer is stale. The mandatory puzzle-triage entry check re-runs on the cumulative evidence after every re-fire.
