# Stage 3a: Empirical Analysis

This file covers two sub-stages: **Gate 3a-feasibility** (empirical feasibility, runs *before* Stage 3 implications) and **Stage 3a** (full empirical analysis, runs *after* Stage 3 implications). In `pipeline_state.json`, use `"stage_3a_feasibility"` during feasibility and `"stage_3a"` during full analysis.

## Preflight: data-source liveness (before any `empiricist` launch in this stage)

`start_services.sh` ran once at session start and produced `output/data_inventory.md`, but a long Stage 2 iteration may have outlived the WRDS session. Before each `empiricist` launch in this stage (Gate 3a-feasibility, Stage 3a plan, Stage 3a execute, and any re-fire), the orchestrator must verify the WRDS server is still reachable:

```bash
PYTHONPATH=code .venv/bin/python3 -c "from utils.wrds_client import wrds_ping; exit(0 if wrds_ping() else 1)"
```

If the first ping returns False, call `wrds_auth_error()` once for the operator-facing diagnosis, then immediately run the exact `wrds_ping()` command above one final time. Honor that final authoritative service result: if it succeeds, continue normally—the first failure or diagnostic result was transient and must not produce a halt. Only if the final ping also returns False may you halt the stage by setting `pipeline_state.json:status = "halted_wrds_unreachable"` and escalate. A non-empty diagnosis means authentication was rejected and the server has latched; an empty diagnosis means the host daemon is absent/unhealthy. In either case **do not restart, call unblock, or run `start_services.sh` from the sandbox**: protected singleton/latch state is deliberately read-only there. The operator restores the service on the host and resumes the run. This preflight is cheap (sub-second when healthy) and prevents the most common silent stall in empirical runs. The same rule applies to the FIX-EMPIRICS empiricist re-launch in `docs/stage_puzzle_triage.md`, the Stage 6 Reject re-fire in `docs/stage_6.md` (which routes through this stage's "Re-fire on theory revision" section), and any future empiricist launch site.

## Specification-lineage paths (every computational launch)

Every quick-feasibility, full, repair, and post-pipeline empirical run also supplies three fresh or immutable paths: `ANALYSIS_BASELINE`, `ANALYSIS_CONTRACT`, and `EXECUTION_SUMMARY`. Use `ANALYSIS_BASELINE = output/analysis_specs/baseline_v1.json` for the first project baseline and reuse it unchanged across retained empirical receipts. After assigning a fresh `ANALYSIS_PATH`, derive `ANALYSIS_CONTRACT = output/analysis_specs/<ANALYSIS_PATH basename without .md>.contract.json` and `EXECUTION_SUMMARY = <ANALYSIS_PATH without .md>_execution.json`; never overwrite either. Thus older instructions below that say to supply “all seven paths” mean those seven plus these three. A genuine baseline revision gets a fresh version and triggers cumulative replacement of every retained old-baseline empirical receipt; paper audit remains blocked until all active empirical v3 receipts share one baseline digest. The plan-only launch still writes only `empirical_plan.md`. At the execution launch, the empiricist translates that approved plan into the baseline/contract before running code, validates it with `analysis_contract.py`, and uses `run-empirical`; the orchestrator and empirics auditor reject any contradiction between plan and contract. The separate offline data-first release continues to use ordinary `run` and has no analysis contract.

## Gate 3a-feasibility: Empirical Feasibility (falsify-first)

Quick falsification check: can this theory be calibrated at all? Do the key empirical moments exist? A theory that predicts the wrong sign on a well-measured moment is dead regardless of how elegant the implications are. Check this BEFORE investing in implications.

**Empirical-first caution.** A wrong-sign feasibility result only kills the idea when the moment is well measured by a proxy/design that actually matches the Stage 1 empirical question. If the mismatch is plausibly due to a proxy, sample, merge-key, aggregation-key, coding, or identification-design artifact, do **not** mark the theory FALSIFIED. Route it as a feasibility-fix: document the artifact in `output/stage3a/empirical_feasibility.md`, repair the proxy/design if possible, and rerun feasibility. If the corrected feasible design no longer identifies the selected idea, use the no-design escalation in `docs/stage_1.md` Step 4 rather than puzzle triage.

<!-- DATA_FIRST_START -->
**Data-first interpretation.** The "theory" is the dataset specification and the "key moments" are the fact portfolio's replication targets: the feasibility question is whether the 2-3 headline replication statistics are computable at all from quick pulls of the named sources (the Stage 1 pilot verified access; this verifies computability of the portfolio's core). A **wrong-sign or wrong-magnitude replication result here is NOT a FALSIFIED verdict** — a failed replication is this mode's highest-value signal, not death: route it through the puzzle-triage entry (`docs/stage_puzzle_triage.md`, data-first note) once the full build exists, and at this gate simply record the discrepancy in the feasibility report and proceed (flag **OK** with the discrepancy noted). FALSIFIED is reserved for a portfolio whose core statistics are genuinely *uncomputable* from the accessible sources — a records/computability failure, which routes back to Stage 1 architectures exactly as written.
<!-- DATA_FIRST_END -->
1. Let T be the current `theory_attempt` and allocate K as the next unused feasibility serial across the entire run (it never resets on a new theory or Regeneration). Set `ANALYSIS_PATH = output/stage3a/empirical_feasibility_t{T}_a{K}.md`, `ANALYSIS_BASELINE = output/analysis_specs/baseline_v1.json`, `ANALYSIS_CONTRACT = output/analysis_specs/empirical_feasibility_t{T}_a{K}.contract.json`, `EXECUTION_SUMMARY = output/stage3a/empirical_feasibility_t{T}_a{K}_execution.json`, `RESULT_PLAN = output/stage3a/empirical_feasibility_t{T}_a{K}_results.plan.json`, `RESULT_BUNDLE = output/stage3a/empirical_feasibility_t{T}_a{K}_results.json`, `RESULT_RECEIPT = output/stage3a/empirical_feasibility_t{T}_a{K}_results.receipt.json`, `ANALYSIS_ENTRYPOINT = code/empirical_feasibility_t{T}_a{K}.py`, `INPUT_SNAPSHOT_DIR = output/stage3a/feasibility_inputs_t{T}_a{K}`, and `SUPERSEDES_ARGS = ()`, substituting the numeric T and K rather than writing braces literally. Copy every mutable document input into that fresh directory before writing `RESULT_PLAN`. Launch `empiricist` with those exact values and a focused instruction: "Quick feasibility check only — download the 2-3 key moments this theory needs to match. Report whether the theory's predictions are in the right ballpark. Do NOT run a full analysis. Write the run plan before execution and the schema-v1 bundle via `RESULTS_BUNDLE_PATH`." If an already accepted feasibility decision for this same theory is intentionally being recomputed, keep it active, set `SUPERSEDES_ARGS` to its exact repeated `--supersedes` pair, and use a fresh K; a rejected pending retry never supersedes anything.
2. Require the report, plan, bundle, and receipt. The feasibility bundle may have no exhibits because it governs a pipeline decision rather than a reader-facing paper claim. Verify it with `python3 code/utils/results_pipeline/results_pipeline.py verify --receipt "$RESULT_RECEIPT" --rerender`; mechanical verification leaves the candidate **pending**. Do not activate until the proxy/design caution and negative-search quality checks below have substantively accepted its decision. Any proxy/design/search-quality repair, execution failure, or verification retry preserves the diagnostic, retires a pending receipt with a non-empty reason, increments K, and allocates a fresh complete `empirical_feasibility_tT_aK` namespace; never rerun prior paths. If a prior accepted feasibility decision is being replaced, it remains active through this routing.
3. Apply the empirical-first caution before treating the sign or magnitude as evidence about the theory. If proxy, sample, merge, aggregation, coding, or identification quality is not adequate, retire the pending candidate and return to step 1 with K+1 and the exact diagnostic. If the candidate declares a moment unavailable, require `output/stage3a/data_search_log.md` to document the query attempts (`list_tables`/`describe_table` for WRDS, provider catalog search for FRED/PWT/OECD/World Bank, canonical alternates tried, WebSearch for renames). A missing or shallow log is likewise a rejected pending candidate: retire it and retry in a fresh namespace. Only a confirmed absence or an adequately measured moment reaches decision routing.
4. Route the substantively accepted decision. If the key moments contradict the theory (wrong sign, off by an order of magnitude) on an adequate proxy/design, flag **FALSIFIED**; otherwise flag **OK** (including a genuinely unavailable moment whose negative search passed). Now activate `RESULT_RECEIPT`; if it replaced a prior accepted feasibility receipt, immediately retire that predecessor with `--superseded-by RESULT_RECEIPT` (this decision gate has no separate state pointer). For **OK**, proceed to Stage 3. For non-seeded **FALSIFIED**, record the accepted falsification, retire its now-active feasibility receipt with the reason that its theory identity is being abandoned, increment `theory_attempt`, reset `theory_version` to 1, and apply the fresh-theory identity reset in `core.md` before returning to Stage 1 (the theory is dead; the counter must advance so the "5 theories on same problem → Stage 0" escalation fires). Don't waste time on implications for a theory the data already rejects.

{{SEED_OVERRIDE_STAGE_3A_FALSIFIED}}
<!-- DATA_FIRST_START -->
**Seeded runs only — data-first reading of the seeded FALSIFIED override.** When `seeded: true`, the seeded-mode override (injected above on seeded deployments) applies with this reading: FALSIFIED at this gate means the fact portfolio's core statistics are **uncomputable** from the accessible sources (see the stage doc's data-first interpretation — a wrong-sign replication is recorded as OK-with-discrepancy and reaches puzzle triage only after the full build). Uncomputability is not a contradiction, so do **not** jump to puzzle triage — there is nothing for the triager to adjudicate. Instead apply the ship-honest check from the seeded Gate 2 override: narrow or explicitly waive the uncomputable classes if the seed-pinned core still ships (document in `output/seed/limitations.md` and proceed), and only if the seed's core classes themselves are uncomputable write `output/seed/abandon_report.md`. Steps 1–4 above apply unchanged when a genuine expectation-vs-data contradiction arrives later from the full build.
<!-- DATA_FIRST_END -->
5. Commit: `artifact: empirical feasibility — {OK/FALSIFIED}`

## Stage 3a: Full Empirical Analysis

This is the full empirical analysis — deeper than the feasibility check at Gate 3a-feasibility. Now that implications are developed, the empiricist can design proper tests, calibrations, and portfolio sorts.

1. **Identification design.**
<!-- THEORY_FIRST_START -->
   Always launch `identification-designer` with the theory, implications, and data inventory — the orchestrator does not pre-judge whether the work will make a causal claim. The designer is the single authority on that question and records its decision as a formal artifact at `output/stage3a/identification_menu.md`, one of:
   - A ranked menu of candidate identification strategies (each with assumptions, diagnostics, estimand, theory-match analysis, anticipated auditor concerns, and software references) when the theory implies one or more causal claims that are testable from the data.
   - `N/A — no causal claim` with a one-paragraph explanation when the empirical work this theory needs is purely calibration, descriptive moment-matching, or model-fit comparison.
   - In the finance variant only, `OUT-OF-SCOPE` with a macro-variant recommendation when the question requires a macro identification toolkit. The macro variant deploys its own designer/auditor and therefore handles SVAR, sign restrictions, narrative shocks, LP-IV, HFI, and structural/calibration questions in scope.

   On `N/A — no causal claim`: record the menu artifact and skip step 3 (the identification audit). The downstream empiricist still writes a plan in step 2; that plan must be consistent with the no-causal-claim record (calibration / descriptive / fit only). On `N/A — no design feasible from the available variation`: do not launch an empiricist plan that pretends the causal object is identified. Treat the prediction as untestable from available data. If the prediction is load-bearing for the paper's claimed contribution, apply the `BACK-TO-IDEA` transition in `docs/stage_puzzle_triage.md` directly, without launching puzzle-triager (there is no accepted result or contradiction for it to triage). Otherwise record the limitation, explicitly reframe the empirical contribution as descriptive/calibration/model-fit, and re-run the designer until it returns `N/A — no causal claim`. On `OUT-OF-SCOPE`: either reframe the empirical work as descriptive / model-fit (which would have produced `N/A` had the theory been written that way), or escalate.
<!-- THEORY_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
   In empirical-first mode the design was produced at Stage 1 (see `docs/stage_1.md` Step 4) and lives at `output/stage1/identification_design.md`. On first pass through Stage 3a, **do not relaunch `identification-designer`** — copy the Stage 1 artifact to `output/stage3a/identification_menu.md` so downstream agents (`empiricist` step 2, `identification-auditor` step 3, `stage_puzzle_triage.md` references) keep their existing canonical-path expectations. Then proceed to step 2.

   Re-launch `identification-designer` only when the "Re-fire on theory revision" trigger conditions apply (the revised mechanism introduces a new causal claim or changes the theoretical object the empirics must identify). On a re-fire, the new menu may overwrite the Stage 1 artifact or be saved to `output/stage3a/identification_menu_vN.md` — at minimum the canonical `output/stage3a/identification_menu.md` must reflect the binding design.

   `N/A` and `OUT-OF-SCOPE` outcomes are not expected in empirical-first mode (the Stage 1 designer would have escalated rather than producing a non-causal artifact); if either appears at re-fire time, treat it as a no-design escalation, not as a puzzle. Launch `branch-manager` with context = `stage-1-empirical-first-no-design` using the revised designer verdict, the current selected idea, the current mechanism, and `data_inventory.md`. Apply the same REENTER-STAGE-1 / REENTER-STAGE-0 / OPERATOR-ESCALATE routing described in `docs/stage_1.md` Step 4. A revised design that becomes non-causal or infeasible is an identification failure, not an empirical contradiction.
<!-- EMPIRICAL_FIRST_END -->
<!-- DATA_FIRST_START -->
**Accepted Gate-2 binding:** in autonomous mode, resolve the rights inventory only from the exact non-null `pipeline_state.json:dataset_rights_inventory` pointer accepted by the current Gate 2. Read the current spec audit's exact `**Coverage certificate:**` and `**Coverage commitments:**` lines and pass them downstream as `COVERAGE_CERTIFICATE_DECISION` / `COVERAGE_COMMITMENTS`. When REQUIRED, also resolve the certificate only from the non-null `pipeline_state.json:dataset_coverage_certificate` pointer, compare its recomputed digest to the exact `pipeline_state.json:dataset_coverage_certificate_sha256` value, and require its `status: PASS`, complete enumeration, and exact current spec/rights path-and-digest bindings before any plan or build launch; when NOT-REQUIRED, the commitments array and both certificate state fields must be empty/null. Any serial-shaped path below denotes that resolved pointer; never reconstruct one from the counter or scan sibling files. In manual mode, there is no pipeline state or Gate 2: the caller supplies the exact inventory path, digest, positive dataset version, `COVERAGE_CERTIFICATE_DECISION`, matching sorted `COVERAGE_COMMITMENTS`, and—when REQUIRED—the exact PASS certificate and digest. Every consumer independently matches the supplied manual decision/array to the spec's `**Commitment IDs:**` line.

**Manual data-first use:** this stage document is a substantive construction/audit reference, not a manual workflow driver. A manual caller supplies every path and decides retries; all `pipeline_state.json` counters, stage pointers, Gate handoffs, and autonomous return routes below are inapplicable. The registry is the receipt handoff. Never create pipeline state to imitate an autonomous step.
   In data-first mode this step is a **no-op**: there is no identification design and the identification agents are not part of this deployment. No menu artifact exists or is created, and its absence is by design, not a defect. The binding upstream artifacts are the accepted dataset specification (autonomous: Gate 2 passed and `dataset_spec_version` is current; manual: caller-accepted), its conditional accepted coverage certificate, and the fact portfolio (`output/stage3/implications.md`). Proceed directly to step 2.
<!-- DATA_FIRST_END -->
<!-- THEORY_FIRST_START -->
2. **Analysis plan.** Launch `empiricist` with instruction: "Write an analysis plan only — do not execute yet." The empiricist reads the theory, implications, data inventory, feasibility results, and (if produced) `identification_menu.md`. If a menu exists, the plan must select a strategy from it or explicitly justify departing from the menu. The empiricist then writes `output/stage3a/empirical_plan.md` describing: what tests to run, what data sources to use — as a `## Source selection` table in `empirical_plan.md` with columns `variable | role | chosen source | alternatives considered | reason chosen dominates | sample cutoff | cutoff citation`. **Load-bearing variables only** (LHS of a headline regression, focal RHS, sort/portfolio-assignment variable, treatment-definition variable, calibration moment): controls and FE buckets get a one-line `source: X` and no further justification. For each load-bearing row, `alternatives considered` lists the finer/longer/more-direct sources from `data_inventory.md` and the wired skills that were checked and rejected; `none — canonical single source` is acceptable for variables with one obvious source (Ken French factors, FRED FEDFUNDS). The `cutoff citation` names the regulation, SEC release, dataset codebook, or prior paper that **defines** the threshold (not merely uses it); intuition-only cutoffs are a fail-loud bug. The `WHY` is the table — do not paraphrase it in prose, what the expected results look like, and what would constitute support vs. rejection of the theory. The plan MUST include a **proxy–theory mapping** section: for each empirical proxy, state (a) which theoretical object it is standing in for, (b) which sub-class / mechanism / scope condition it captures (if the theory has heterogeneous agent types, scope-conditional predictions, or multiple mechanisms), (c) whether the proxy construction is mechanically correlated with the theoretical object (e.g., HHI as proxy for concentration-driven noise variance is mechanical; tracking-error is not), and (d) a non-mechanical alternative proxy. If the theory has multiple sub-classes, the plan must include **at least one proxy per sub-class in the primary analysis** — not deferred to robustness. If the main proxy is mechanically correlated with its theoretical object, the non-mechanical alternative is mandatory in the primary analysis as well.
<!-- THEORY_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
2. **Analysis plan.** Launch `empiricist` with instruction: "Write an analysis plan only — do not execute yet." The empiricist reads the theory, implications, data inventory, feasibility results, and (if produced) `identification_menu.md`. If a menu exists, the plan must select a strategy from it or explicitly justify departing from the menu. The empiricist then writes `output/stage3a/empirical_plan.md` describing: what tests to run, what data sources to use — as a `## Source selection` table in `empirical_plan.md` with columns `variable | role | chosen source | alternatives considered | reason chosen dominates | sample cutoff | cutoff citation`. **Load-bearing variables only** (LHS of a headline regression, focal RHS, sort/portfolio-assignment variable, treatment-definition variable, calibration moment): controls and FE buckets get a one-line `source: X` and no further justification. For each load-bearing row, `alternatives considered` lists the finer/longer/more-direct sources from `data_inventory.md` and the wired skills that were checked and rejected; `none — canonical single source` is acceptable for variables with one obvious source (Ken French factors, FRED FEDFUNDS). The `cutoff citation` names the regulation, SEC release, dataset codebook, or prior paper that **defines** the threshold (not merely uses it); intuition-only cutoffs are a fail-loud bug. The `WHY` is the table — do not paraphrase it in prose, what the expected results look like, and what would constitute support vs. rejection of the theory. The plan MUST include a **proxy–theory mapping** section: for each empirical proxy, state (a) which theoretical object it is standing in for, (b) which sub-class / mechanism / scope condition it captures (if the theory has heterogeneous agent types, scope-conditional predictions, or multiple mechanisms), (c) whether the proxy construction is mechanically correlated with the theoretical object (e.g., HHI as proxy for concentration-driven noise variance is mechanical; tracking-error is not), and (d) a non-mechanical alternative proxy. If the theory has multiple sub-classes, the plan must include **at least one proxy per sub-class in the primary analysis** — not deferred to robustness. If the main proxy is mechanically correlated with its theoretical object, the non-mechanical alternative is mandatory in the primary analysis as well.
   When the design was set (or revised) by `identification-designer`, `identification_menu.md` already carries a per-strategy **Source selection (load-bearing design variables)** mini-table for the variables the identification hinges on (running/forcing variable, treatment indicator, instrument, focal LHS/RHS). For those variables, copy the designer's three pre-filled cells (`chosen source`, `sample cutoff`, `cutoff citation`) into the matching row of `empirical_plan.md`'s `## Source selection` table **verbatim** — they are already anchored at the design point and must not be re-derived. The designer's `role in design` column maps to the plan's `role` column; only the plan-specific `alternatives considered` and `reason chosen dominates` columns need filling in. (On a Stage 3a designer re-fire, re-copy from the updated menu the same way.) This anchors citations and cutoffs to the design and gives `data-selection-auditor` a cross-check (designer's table vs. plan's table must agree). Non-design variables (controls, FE buckets, auxiliary covariates) are not in the designer's table — source them as usual.
<!-- EMPIRICAL_FIRST_END -->
<!-- DATA_FIRST_START -->
2. **Construction-and-portfolio plan.** Launch `empiricist` with instruction: "Write a construction-and-portfolio plan only — do not execute yet." The empiricist reads the dataset specification (`output/stage2/theory_draft_vN.md`), its accepted machine-readable rights inventory (`output/stage2/source_rights_s{dataset_spec_serial}_vN.json`), the conditional accepted coverage certificate, the fact portfolio (`output/stage3/implications.md`), the data inventory, the Stage 1 pilot report, and the feasibility results, then writes `output/stage3a/empirical_plan.md` with four parts. **(i) Analysis build plan:** per event class, the pull/parse/normalize steps that implement the spec's inclusion, dating, and reconciliation rules — citing the spec's rule for each step, never inventing one; where a rule proves unimplementable as written, the plan flags it as a spec gap (routes to a Gate 2 re-fire) rather than silently substituting. When a coverage certificate exists, its complete event-key set is a binding construction input: account for every certified key and explain any source drift since the census rather than silently dropping a row. The `## Source selection` table is filled from the spec's source inventory **verbatim** for every event class (primary and second source per class in the `chosen source` / `alternatives considered` columns; the exact `source_id`, rights classification, and rule basis in the `cutoff citation` column) — the spec is the design point and must not be re-derived; this gives `data-selection-auditor` its cross-check. **(ii) Validation plan:** the triangulation execution — per class, the second-source cross-check procedure over the full claimed span, the reconciliation-log path and format from the spec, and the explicit waiver list for single-sourced classes. **(iii) Portfolio plan:** one entry per fact-portfolio item — the exact statistic, sample window, comparison, expected value (for replications, with citation), and the construction-sensitivity re-runs for each headline candidate. **(iv) Separate release plan:** after the networked analysis producer finishes, a distinct offline producer receives only rights-cleared cached inputs plus control documents and writes one fresh `output/dataset/release_vN_aK/` artifact. The plan names the release entrypoint, immutable rights inventory, input-provenance JSON, run plan/bundle/receipt, schema documentation, build entrypoints, and exact release-manifest fields required by step 5.5. Restricted inputs may feed the analysis and build-from-source instructions but are never exposed to this offline producer. The plan MUST include a **portfolio–schema mapping** section: for each item, which event classes and columns it consumes, and whether the computed statistic is mechanically sensitive to a construction convention (dating rule, dedup window, reconciliation priority) — every mechanically-sensitive headline item requires its sensitivity re-run in the primary analysis, not deferred to robustness.
<!-- DATA_FIRST_END -->
<!-- THEORY_FIRST_START -->
3. **Identification audit.** If step 1's designer artifact is a strategy menu (not `N/A` and not `OUT-OF-SCOPE`), launch `identification-auditor` on `empirical_plan.md`. The auditor returns PASS / REVISE / FAIL with severity-ranked named failure modes (e.g., `TWFE-staggered-no-robust-estimator`, `weak-iv-stockyogo-misuse`, `no-rd-manipulation-test`) and an estimand-vs-theory match analysis. Saves to `output/stage3a/identification_audit.md`. (The auditor also has a self-check that returns PASS-N/A if the empiricist's plan turns out to make no causal claim despite the designer producing a menu — this is a safety net for cases where the empiricist scoped down between step 1 and step 2; the designer remains the canonical decider.)
   - If **PASS**: proceed to step 4 (orchestrator plan review).
   - If **PASS-N/A** (auditor safety-net — empiricist scoped down to a non-causal plan despite the designer issuing a menu): proceed as if PASS for routing purposes; the designer's menu remains the canonical record of what the theory implied identification-wise, and the auditor's PASS-N/A flags that the executed plan ended up narrower. The orchestrator should re-read the plan in step 4 and verify it is genuinely non-causal (the empiricist's plan, not the auditor's read, is binding); if any causal claim sneaks back into the plan after this verdict, kick it back to the empiricist for a plan that is consistent with the auditor's read.
   - If **REVISE**: re-launch `empiricist` with the audit's failure-mode list and severity-7+ fixes. The empiricist revises the plan in place. Re-run the auditor. **Increment `pipeline_state.json:loops.identification_plan_revision.round`** on each REVISE → re-plan cycle. **Hard cap: 3 plan-revision rounds.** If `loops.identification_plan_revision.round >= loops.identification_plan_revision.cap` and the auditor still returns REVISE, treat as FAIL. Reset the counter to 0 on PASS, on FAIL routing to a different strategy from the menu, or on Stage 3a re-fire entry per "Re-fire on theory revision."
   - If **FAIL**: the chosen design class cannot identify the theoretical object from this data. Reset `loops.identification_plan_revision.round` to 0 **AND reset `loops.method_check.round` AND `loops.data_integrity.round` to 0** — a strategy switch produces fresh code under different estimators (DiD → RDD changes the canonical-package universe entirely) and typically a fresh data slice; the prior counters from the abandoned strategy must not bleed across or they will premature-cap-FAIL legitimate first-pass REVISEs on the new strategy's code. Re-launch `empiricist` with the auditor's verdict and the designer's full menu, instructing the empiricist to select a different strategy. If all strategies in the menu have been exhausted or the designer returned `N/A — no design feasible`, treat the prediction as untestable from available data. If it is load-bearing, apply the `BACK-TO-IDEA` transition in `docs/stage_puzzle_triage.md` directly without launching the triager; otherwise record the limitation, reframe the empirical work as descriptive/calibration/model-fit, and re-enter at step 1 until the designer returns `N/A — no causal claim`. **Empirical-first exception:** because empirical-first commits to a causal design at Stage 1, exhausted or infeasible strategies are a no-design escalation; use the branch-manager routing in `docs/stage_1.md` Step 4.
   - If **OUT-OF-SCOPE** (macro design): see step 1 handling.
   - Skip this step entirely if step 1 returned `N/A` (no causal claim).
<!-- THEORY_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
3. **Identification audit.** If step 1's designer artifact is a strategy menu (not `N/A` and not `OUT-OF-SCOPE`), launch `identification-auditor` on `empirical_plan.md`. The auditor returns PASS / REVISE / FAIL with severity-ranked named failure modes (e.g., `TWFE-staggered-no-robust-estimator`, `weak-iv-stockyogo-misuse`, `no-rd-manipulation-test`) and an estimand-vs-theory match analysis. Saves to `output/stage3a/identification_audit.md`. (The auditor also has a self-check that returns PASS-N/A if the empiricist's plan turns out to make no causal claim despite the designer producing a menu — this is a safety net for cases where the empiricist scoped down between step 1 and step 2; the designer remains the canonical decider.)
   - If **PASS**: proceed to step 4 (orchestrator plan review).
   - If **PASS-N/A** (auditor safety-net — empiricist scoped down to a non-causal plan despite the designer issuing a menu): proceed as if PASS for routing purposes; the designer's menu remains the canonical record of what the theory implied identification-wise, and the auditor's PASS-N/A flags that the executed plan ended up narrower. The orchestrator should re-read the plan in step 4 and verify it is genuinely non-causal (the empiricist's plan, not the auditor's read, is binding); if any causal claim sneaks back into the plan after this verdict, kick it back to the empiricist for a plan that is consistent with the auditor's read.
   - If **REVISE**: re-launch `empiricist` with the audit's failure-mode list and severity-7+ fixes. The empiricist revises the plan in place. Re-run the auditor. **Increment `pipeline_state.json:loops.identification_plan_revision.round`** on each REVISE → re-plan cycle. **Hard cap: 3 plan-revision rounds.** If `loops.identification_plan_revision.round >= loops.identification_plan_revision.cap` and the auditor still returns REVISE, treat as FAIL. Reset the counter to 0 on PASS, on FAIL routing to a different strategy from the menu, or on Stage 3a re-fire entry per "Re-fire on theory revision."
   - If **FAIL**: the chosen design class cannot identify the theoretical object from this data. Reset `loops.identification_plan_revision.round` to 0 **AND reset `loops.method_check.round` AND `loops.data_integrity.round` to 0** — a strategy switch produces fresh code under different estimators (DiD → RDD changes the canonical-package universe entirely) and typically a fresh data slice; the prior counters from the abandoned strategy must not bleed across or they will premature-cap-FAIL legitimate first-pass REVISEs on the new strategy's code. Re-launch `empiricist` with the auditor's verdict and the designer's full menu, instructing the empiricist to select a different strategy. If all strategies in the menu have been exhausted or the designer returned `N/A — no design feasible`, treat the prediction as untestable from available data. If it is load-bearing, apply the `BACK-TO-IDEA` transition in `docs/stage_puzzle_triage.md` directly without launching the triager; otherwise record the limitation, reframe the empirical work as descriptive/calibration/model-fit, and re-enter at step 1 until the designer returns `N/A — no causal claim`. **Empirical-first exception:** because empirical-first commits to a causal design at Stage 1, exhausted or infeasible strategies are a no-design escalation; use the branch-manager routing in `docs/stage_1.md` Step 4.
   - If **OUT-OF-SCOPE** (macro design): see step 1 handling.
   - Skip this step entirely if step 1 returned `N/A` (no causal claim).
<!-- EMPIRICAL_FIRST_END -->
<!-- DATA_FIRST_START -->
3. **Identification audit — never fires in data-first mode.** There is no designer artifact to audit and the auditing agent is not part of this deployment. The built dataset's audit chain is steps 6.5–7.6: headline replication, empirics audit (whose data-first block checks spec conformance), the step-7.5 triad, and the step-7.6 coverage audit. Proceed to step 4.
<!-- DATA_FIRST_END -->
<!-- THEORY_FIRST_START -->
4. **Review the plan.** Read the plan. Check: does it use the best available data? (If WRDS is available but the plan uses only CZ portfolios, reject the plan.) Does it test what the theory actually predicts? **Does every theoretical sub-class have at least one empirical proxy in the primary analysis?** Identification soundness is already gated by step 3 — focus this review on data-choice and proxy-theory mapping concerns the auditor doesn't cover. If the plan is wrong on these dimensions, re-launch the empiricist with specific feedback.
<!-- THEORY_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
4. **Review the plan.** Read the plan. Check: does it use the best available data? (If WRDS is available but the plan uses only CZ portfolios, reject the plan.) Does it test what the theory actually predicts? **Does every theoretical sub-class have at least one empirical proxy in the primary analysis?** Identification soundness is already gated by step 3 — focus this review on data-choice and proxy-theory mapping concerns the auditor doesn't cover. If the plan is wrong on these dimensions, re-launch the empiricist with specific feedback.
<!-- EMPIRICAL_FIRST_END -->
<!-- DATA_FIRST_START -->
4. **Review the plan.** Read the plan. Check: does the analysis build plan implement every spec rule (no invented rules, no silently dropped ones)? Does the `## Source selection` table match the prose and JSON source inventories verbatim? Does the validation plan cover every non-waived event class over its full claimed span, with the reconciliation-log format the spec defines? Does the portfolio plan cover **every** fact-portfolio item, with expected values on the replications and sensitivity re-runs on every mechanically-sensitive headline candidate? Does the separate release plan make restricted data absent by construction — fresh versioned release directory, offline run, no provider credentials, every producer input classified by `release_inputs_vN_aK.json`, every data source `open` in the exact rights inventory selected by the release authority, and a complete checksummed manifest? The networked analysis run must declare no artifact beneath `output/dataset/`; the trusted runner rejects such an artifact without a release contract. Spec conformance of the built dataset is gated later (step 7's data-first conformance block and the step-7.5/7.6 auditors) — focus this review on plan-vs-spec and portfolio-coverage concerns those audits assume settled. If the plan is wrong on these dimensions, re-launch the empiricist with specific feedback; if the plan exposes a genuine spec gap, route to a Gate 2 spec-audit re-fire instead.
<!-- DATA_FIRST_END -->
<!-- DATA_FIRST_START -->
**Data-first execution preflight (before step 5):** set `RESULT_PLAN.requires_dataset_release = true` before the analysis producer runs. The resulting immutable analysis receipt must carry this guard; setting it after execution is too late and the trusted release runner will reject the pair.
<!-- DATA_FIRST_END -->
5. **Execute.** On the first-ever full Stage 3a run (both `stage3a_result_receipt` and `stage3a_analysis_path` are null), set `ANALYSIS_PATH = output/stage3a/empirical_analysis.md`, `ANALYSIS_BASELINE = output/analysis_specs/baseline_v1.json`, `ANALYSIS_CONTRACT = output/analysis_specs/empirical_analysis.contract.json`, `EXECUTION_SUMMARY = output/stage3a/empirical_analysis_execution.json`, `RESULT_PLAN = output/stage3a/empirical_analysis_results.plan.json`, `RESULT_BUNDLE = output/stage3a/empirical_analysis_results.json`, `RESULT_RECEIPT = output/stage3a/empirical_analysis_results.receipt.json`, `ANALYSIS_ENTRYPOINT = code/empirical.py`, `RENDER_ENTRYPOINT = code/render_empirical_exhibits.py`, `INPUT_SNAPSHOT_DIR = output/stage3a/analysis_inputs_a1`, and `SUPERSEDES_ARGS = ()`. If a retained active receipt exists with a null/stale acceptance version after a theory-identity reset, use the cumulative-replacement procedure and its supersession array. Copy the approved empirical plan, theory, implications, identification artifacts, and other mutable document inputs into the fresh snapshot before writing `RESULT_PLAN`; launch `empiricist` with the exact values and copies. The agent executes the plan, fetches data via skills (FRED, Ken French, Chen-Zimmerman, WRDS, EDGAR), and runs the analysis through the results utility. The analysis report MUST include a `## Headline claims` section with 1-5 one-line numerical claims carrying `[HEADLINE]`, snake_case `[claim_id: …]`, exact raw-unit `[reported_value: …]`, and one fixed `[tolerance_class: …]` tag — these are the load-bearing values and policies the next step mechanically binds and replicates. Require `python3 code/utils/results_pipeline/results_pipeline.py verify --receipt "$RESULT_RECEIPT" --rerender` to pass before step 6.5; a nonzero exit re-fires the empiricist and cannot be deferred.

   **Fresh-attempt transition for every later empiricist repair in this stage:** before any branch below re-launches `empiricist`, preserve its diagnostic, explicitly retire the current pending `RESULT_RECEIPT` with that reason, increment K, and assign fresh versioned `ANALYSIS_PATH`, `ANALYSIS_CONTRACT` (derived from that report basename under `output/analysis_specs/`), `EXECUTION_SUMMARY` (the report path with `_execution.json` replacing `.md`), `RESULT_PLAN`, `RESULT_BUNDLE`, `RESULT_RECEIPT`, analysis/renderer entrypoints, input snapshot, artifact, and exhibit paths; retain the exact immutable `ANALYSIS_BASELINE` unless this is the explicit cumulative baseline migration. Copy the current approved documents into the fresh snapshot, run the full analysis→bundle→render→verify sequence, then continue at step 6.5 on the new candidate. If a prior accepted Stage 3a receipt exists, this is a **cumulative replacement**, not a partial targeted bundle: declare its immutable report, bundle, receipt, and any still-needed artifacts as inputs; reproduce or carry forward every still-valid result/result ID and exhibit in the fresh namespace; add or recompute the repaired results; set `SUPERSEDES_ARGS` to one repeated `--supersedes <receipt>` pair for every absorbed active Stage 3a receipt, and supply that exact array to the producer. With no active predecessor, set it to the empty array. The new `ANALYSIS_PATH` must be a self-contained cumulative report, so the single Stage 3a state pair never drops earlier accepted coverage. Each predecessor remains active throughout verification and every auditor. Explicit activation after final PASS adds the replacement, and only the subsequent three-field state-pointer handoff plus explicit retirement displaces the absorbed receipts. Report tags, rebuttals, docstrings, code, data, estimates, bundles, artifacts, and exhibits are all receipt-bound: none may be edited in place. Any halt or return to Stage 2 first retires the current pending receipt with its terminal diagnostic. These explicit lifecycle steps apply to every repair/cap branch below.
<!-- DATA_FIRST_START -->
   **Data-first paired-attempt rule:** the same K owns a separate release candidate. Before any repair re-launch, also preserve the release diagnostic and, **only if the release run published `RELEASE_RECEIPT` as pending**, retire that pending receipt; a failure before release publication has no release receipt to retire. Allocate fresh `RELEASE_PATH`, `RELEASE_PLAN`, `RELEASE_BUNDLE`, `RELEASE_RECEIPT`, `RELEASE_ENTRYPOINT`, `RELEASE_INPUT_PROVENANCE`, and rights-cleared input-snapshot paths for K+1. The prior accepted release receipt remains active and `dataset_release_path` / `dataset_release_receipt` remain unchanged until the replacement passes the audit quartet. Never reuse a retired release directory or receipt namespace.

   **Release supersession and cleanup:** maintain `RELEASE_SUPERSEDES_ARGS` independently from the analysis `SUPERSEDES_ARGS`. It is empty on the first accepted pair. When `pipeline_state.json:dataset_release_receipt` names the active predecessor being cumulatively replaced, set it to exactly `--supersedes "$dataset_release_receipt"` and pass it to the release `results_pipeline.py run`; never copy the analysis supersession array into the release run. After `RELEASE_RECEIPT` has been published pending, every later instruction in this stage to retire `RESULT_RECEIPT`—repair, cap, halt, source outage, or return to Stage 2—means atomically retire both members with `results_pipeline.py retire-pair --analysis-receipt "$RESULT_RECEIPT" --release-receipt "$RELEASE_RECEIPT" --reason "<diagnostic>"`. The same command retires an active pair being abandoned. Ordinary `retire` is mechanically rejected for either pending or active paired member. Before release publication, retire the lone pending analysis normally. This rule prevents every terminal/fallback route from stranding half a pair.

5.5. **Build the release through the trusted offline boundary (data-first only).** Set:

   - `RIGHTS_INVENTORY = pipeline_state.json:dataset_rights_inventory` (the exact non-null pointer accepted by the current Gate 2; do not reconstruct it from counters or scan sibling files)
   - `RIGHTS_INVENTORY_SHA256 = pipeline_state.json:dataset_rights_inventory_sha256` (the exact accepted digest; recompute the inventory digest and require equality before writing the plan)
   - `RIGHTS_AUTHORITY = "gate2-state"`
   - `RELEASE_PATH = output/dataset/release_v{theory_version}_aK`
   - `RELEASE_PLAN = output/stage3a/dataset_release_v{theory_version}_aK_results.plan.json`
   - `RELEASE_BUNDLE = output/stage3a/dataset_release_v{theory_version}_aK_results.json`
   - `RELEASE_RECEIPT = output/stage3a/dataset_release_v{theory_version}_aK_results.receipt.json`
   - `RELEASE_ENTRYPOINT = code/build_dataset_release_v{theory_version}_aK.py`
   - `RELEASE_INPUT_PROVENANCE = output/stage3a/release_inputs_v{theory_version}_aK.json`

   Those are the autonomous defaults. In a `--manual` data-first deployment, there is no Gate 2 state and no `pipeline_state.json`: the caller supplies the exact inventory path, SHA-256, positive dataset version, release paths, and substantive acceptance, and sets `RIGHTS_AUTHORITY = "manual-caller"`. The trusted runner reads `.deploy_manifest.json` and rejects `manual-caller` in an autonomous deployment, `gate2-state` in a manual deployment, or an invented manual `pipeline_state.json`. Manual mode still receives the same offline, provenance, manifest, receipt-pair, and audit enforcement; caller authority replaces only autonomous Gate-2 sequencing.

   The release is **not** part of `RESULT_PLAN` / `RESULT_RECEIPT`; the analysis producer may use networked or restricted inputs but may not declare any output beneath `output/dataset/`. Prepare fresh immutable input copies for the release builder. `RELEASE_INPUT_PROVENANCE` has schema version 1, the current dataset version, the exact `RIGHTS_INVENTORY` path, and one row for every release producer input other than the rights/provenance files: `{"path": ..., "role": "data"|"control", "source_ids": [...]}`. Every data row has ≥1 exact source ID and every one is `open` in the accepted rights inventory; control rows have no source IDs. Include `RESULT_RECEIPT`, the rights inventory, and the provenance file in `RELEASE_PLAN.producer_inputs`; classify `RESULT_RECEIPT` as a control input and classify every other non-manifest producer input exactly once. `RESULT_RECEIPT` must still be the sole pending receipt and must already pass `verify --rerender`; the trusted runner permits only its bound release run while that receipt is pending. Set `network_access: false` and select no provider credentials.

   `RELEASE_PLAN.dataset_release` must be:

   ```json
   {
     "artifact": "RELEASE_PATH",
     "manifest": "RELEASE_PATH/manifest.json",
     "rights_inventory": "RIGHTS_INVENTORY",
     "rights_inventory_sha256": "RIGHTS_INVENTORY_SHA256",
     "rights_authority": "RIGHTS_AUTHORITY",
     "input_provenance": "RELEASE_INPUT_PROVENANCE",
     "dataset_version": 1,
     "analysis_receipt": "RESULT_RECEIPT",
     "producing_receipt": "RELEASE_RECEIPT"
   }
   ```

   Replace the symbolic strings and `dataset_version` with the exact paths, accepted inventory digest, selected authority, and current autonomous `theory_version` or caller-supplied manual dataset version before writing JSON. The producer emits `RELEASE_PATH/manifest.json` with schema/version, the exact analysis receipt plus release-receipt/inventory/provenance paths, the exact `rights_inventory_sha256` and `rights_authority`, `build_sources`, `build_entrypoints`, `schema_document`, and a `files` row for every other regular file: relative path, kind (`data`, `code`, or `documentation`), `sha256:<hex>`, and source IDs (non-empty and open for data; empty for code/docs). It must include at least one redistributable data file. Run `results_pipeline.py run` with `RELEASE_PLAN` / `RELEASE_BUNDLE` / `RELEASE_RECEIPT` and `--caller-allowance-seconds` set to the tracked long-running job's real wall-clock allowance (minimum 1200; the runner refuses to start without it), then `verify` that receipt and require exit zero with JSON `status: "PASS"`. The trusted parent checks the plan's rights path/hash/version against the selected autonomous or manual authority, runs the producer with network denied, rejects restricted or unmapped inputs, checks every output source ID and checksum, and publishes `RELEASE_PATH` only after all checks pass. A failure leaves no release directory in the project tree; repair through the paired fresh-attempt transition, never by copying files around the runner.
   `build_sources` maps every `RELEASE_PLAN.producer_code` path exactly once to the same relative path inside the release, preserving the producer's module layout. Each target must be byte-identical to its source, there may be no other manifested code files, and `build_entrypoints` must include the path-preserved producer command's actual `RELEASE_ENTRYPOINT`. The trusted runner therefore rejects missing or renamed **declared** helpers, undeclared packaged code, and ordinary source-code paths misclassified as inputs. It cannot infer arbitrary dynamically loaded dependencies or whether declared code is reachable: the empirics audit must inspect imports/load calls and confirm that every runtime code dependency is in `producer_code`/`build_sources`. The release `results_pipeline.py run` invocation must include the exact `RELEASE_SUPERSEDES_ARGS` defined above, even when the array is empty.
<!-- DATA_FIRST_END -->
6. All code must be written to files (`code/` for final, `code/tmp/` for scratch). Never run inline `python3 -c`.
6.5. **Headline replication (independent path).** Set `ANALYSIS_PATH` to the analysis this firing must verify: `output/stage3a/empirical_analysis.md` on the first pass, or the explicit versioned path named by "Re-fire on theory revision" below. Run `python3 -I -S code/utils/empirical_input_manifest.py paths --analysis ANALYSIS_PATH` and take its exact `verify_script`, `verify_result`, and `pass_candidate` values as `VERIFY_SCRIPT_PATH`, `VERIFY_RESULT_PATH`, and `PASS_CANDIDATE_PATH`. Use the same `-I -S` isolation flags for every invocation of this utility below so project modules cannot shadow the standard library. Before every launch or re-launch, capture any prior diagnostic needed for retry feedback, then remove only the exact derived `VERIFY_SCRIPT_PATH`, `VERIFY_RESULT_PATH`, and `PASS_CANDIDATE_PATH` for this analysis (never a sibling artifact), so stale output cannot impersonate the new firing. Launch `headline-replicator` on all four exact paths + `code/` + the raw data sources. For each `[HEADLINE]`-tagged claim the agent recomputes the value via a DIFFERENT aggregation path. A PASS is written only by `empirical_input_manifest.py finalize-pass`, which requires identical bound-input manifests before and after its one verifier run, derives claim values/deltas/fixed tolerances, and atomically binds the result to that exact canonical or versioned analysis, verifier script, and every regular project file under `code/` including executable payloads under `__pycache__/`. Later freshness comparisons are hash-only and do not rerun the verifier. The agent self-FAILs on absent/non-running script, malformed/unagreed claim evidence, trivially-equivalent path, missing required headline tags, or any claim disagreeing beyond its bound tolerance.

   **Pre-routing file + input-binding check.** After the agent returns, if `PASS_CANDIDATE_PATH` exists, the orchestrator—not the agent—must run `python3 -I -S code/utils/empirical_input_manifest.py finalize-pass --analysis ANALYSIS_PATH --candidate PASS_CANDIDATE_PATH --result VERIFY_RESULT_PATH`; only its zero exit creates a prospective PASS. If no candidate exists, require a fresh `VERIFY_RESULT_PATH` whose verdict is FAIL. An agent-authored PASS without a candidate, simultaneous candidate/result outputs, or a finalization error is a replicator self-failure. Before evaluating the routing, verify `VERIFY_RESULT_PATH` exists on disk and parses as JSON. If it reports PASS, run `python3 -I -S code/utils/empirical_input_manifest.py compare --result VERIFY_RESULT_PATH --analysis ANALYSIS_PATH` and require a zero exit plus JSON `status: "UNCHANGED"`. This verifies both that the manifest names the file the replicator was launched on and that its content/code hashes remain current. A missing, malformed, internally inconsistent, wrong-analysis, or already-stale `input_manifest` is a replicator self-failure; a PASS that does not bind to the exact live inputs is not a PASS. If the result file is absent/malformed or this PASS check fails, use the same self-failure loop as `trivially_equivalent_path`: **increment `loops.replicator_self_refire.round`; if `loops.replicator_self_refire.round >= loops.replicator_self_refire.cap`, retire the pending `RESULT_RECEIPT` with the final self-failure diagnostic and halt** with `pipeline_state.json:status = "halted_replicator_self_failure"`; otherwise re-launch `headline-replicator` on the same `ANALYSIS_PATH`, `VERIFY_SCRIPT_PATH`, `VERIFY_RESULT_PATH`, and `PASS_CANDIDATE_PATH`, with the parse/comparison diagnostic and an instruction to emit a valid result bound to those exact paths and the current code. With the default cap 3 this tolerates three failed firings before halting. (This is a real capped loop with backing state. Its reset scope is the current replicator attempt on the current headline: a valid replicator verdict or a new empiricist headline zeroes it per the generic Audit-loop scoping rule.) Do not infer a default verdict from an empty or missing file.

   **Routing.** Evaluate the conditions below in order; the first match wins. (The conditions are mutually exclusive by construction — a `verdict: PASS` cannot co-occur with substantive failures, and the warning-driven cases are dispatched before the substantive ones so an over-tagged-but-still-disagreeing run is correctly routed as substantive.)
   - If **`verdict: PASS`**: run the all-analysis freshness gate below, then proceed to step 7. The `over_tagging` and `untagged_headline_*` warnings (if present) are informational only at this step — they get forwarded to empirics-auditor for inclusion in its report, and the empiricist may revise tagging on the next iteration if the auditor flags it.
   - If **`verdict: FAIL` AND `"source_unreachable" ∈ untagged_warnings`** (replicator could not pull from the raw data source; `claims` is empty): retry the data-source preflight at the top of this file once. If the source comes back, re-fire `headline-replicator` on the same immutable candidate. If still unreachable, retire the pending `RESULT_RECEIPT` with the outage diagnostic, set `pipeline_state.json:status = "halted_data_audit_unreachable"`, and stop. Do not increment any counter.
   - If **`verdict: FAIL` AND `"no_headline_tags" ∈ untagged_warnings`** (empiricist forgot to tag; `claims` is empty): use the fresh-attempt transition above with the diagnostic and instruct the new empiricist attempt to add the missing `[HEADLINE]` tags while reproducing the same computation. Tagging does **not** increment the step 7 audit-fix counter or `loops.headline_replication.round`; the new verified candidate proceeds through step 6.5 normally.
   - If **`verdict: FAIL` AND `"trivially_equivalent_path" ∈ untagged_warnings`** (replicator emitted a banned trivial path): replicator-self-failure, not an empiricist bug; do **not** increment the step 7 audit-fix counter or `loops.headline_replication.round`. Increment `loops.replicator_self_refire.round` (the same self-refire loop as the crash case in the pre-routing file check above); **if `loops.replicator_self_refire.round >= loops.replicator_self_refire.cap`, retire the pending receipt with the replicator diagnostic and halt** with `pipeline_state.json:status = "halted_replicator_self_failure"` for operator review; otherwise re-launch `headline-replicator` on the same immutable candidate with the prior bad output as feedback (cap 3 → three failed firings tolerated before halting).
   - If **`verdict: FAIL` AND any claim row has `agree: false`** (substantive disagreement on a headline number; over-tagging warning may co-occur and is forwarded as informational): increment `pipeline_state.json:loops.headline_replication.round`, then use the fresh-attempt transition with the `VERIFY_RESULT_PATH` payload and instruct the new attempt: "Either correct the headline value(s)/empirical code to match the replicated value, or attach a rebuttal note to the disputed `[HEADLINE]` line formatted as `[REBUTTAL claim_id: <id>] <why the prior verification path was incorrect> [verification-redesign suggestion: <proposed alternative path>]`. A rebuttal must cite the specific lines in `VERIFY_SCRIPT_PATH` it disputes." Run headline replication against the new `ANALYSIS_PATH` and its newly derived verifier paths. **Hard cap: 3 headline-replication rounds.** At the cap, retire the pending receipt with the disagreement before returning to Stage 2 (the headline is unverifiable as written). **Counter resets for `loops.headline_replication.round`** (all set to 0): on PASS at this step, on the `no_headline_tags` / `source_unreachable` / `trivially_equivalent_path` routings above (those are not substantive disagreement rounds), on re-execution from a substantive data FAIL at step 7.5, on any mandatory or manifest-triggered re-fire after a downstream empiricist repair, and on Stage 3a re-fire entry per "Re-fire on theory revision."
   - If **`verdict: FAIL` AND none of the above match** (no warnings indicating a known failure mode; no `agree: false` claim row): treat as replicator-self-failure of an unrecognized class. Re-launch `headline-replicator` once on the same `ANALYSIS_PATH` with an instruction to investigate and either emit a recognized failure mode or PASS bound to that exact path. Do **not** increment any counter. If the next firing also lands in this branch, retire the pending `RESULT_RECEIPT` with both unrecognized diagnostics, then halt with `pipeline_state.json:status = "halted_replicator_unrecognized_failure"` for operator review — this signals a contract gap between the agent body and the routing.
   **All-analysis freshness gate.** Each analysis owns distinct verifier artifacts, so a later repair cannot erase its evidence. After the primary firing reports PASS, run `python3 -I -S code/utils/empirical_input_manifest.py check-all`. This resolves canonical/versioned `output/stage3a/empirical_analysis*.md` ownership from `process_log/results_registry.json` and each receipt's `producer_run.artifacts`, checks only active and pending analyses against the final complete `code/` surface, and lists retired analyses as `EXCLUDED_RETIRED` without re-verifying them. A real regular `empirical_analysis*_results.plan.json` that no results receipt references is only a pre-publication control input—either awaiting its runner or left by a runner that failed before publication—so `check-all` reports it in top-level `warnings` without changing the verdict; the plan cannot make evidence live, and any later runner invocation must still validate it before publishing a receipt. A missing/malformed registry or receipt, an unregistered/duplicate/missing analysis, an undeclared bundle/receipt/execution summary, or any non-regular reserved-namespace object appears in top-level `artifact_errors`; if that array is nonempty, do not enter the refresh batch or delete an ambiguous artifact: set `pipeline_state.json:status = "halted_replication_artifact_collision"`, record every exact path/error for operator recovery, and stop. This bounded safety halt prevents an orphan or invalid reserved-namespace object from causing an infinite re-fire loop without letting an inert plan halt unrelated later attempts. Otherwise, if any active/pending analysis entry is `CHANGED` or `ERROR`, sequentially re-fire `headline-replicator` for each named analysis with the `verify_script`/`verify_result`/`pass_candidate` paths in that entry (normal disagreement/self-failure routing applies). **Data/cache invalidation mode:** when the branch that entered step 6.5 rebuilt, re-fetched, re-filtered, or otherwise changed empirical data/cache bytes, set `FORCE_ALL_ANALYSES = true` and re-fire every active/pending analysis unconditionally even if `check-all` says its code/headline hashes are unchanged—the manifest deliberately excludes data. Never re-fire an `EXCLUDED_RETIRED` entry. After either batch, run `check-all` again. Do not enter step 7 until its top-level status is `UNCHANGED` and every forced firing PASSes. During a batch, defer recursive all-analysis checks until every stale/forced entry has been refreshed; verifier scripts live outside `code/`, so successful sibling revalidations converge without invalidating one another.

   **Stored-input guard for code/headline-only downstream repairs.** Where a later branch explicitly invokes this guard, before reusing the current replicator PASS run `compare --result VERIFY_RESULT_PATH --analysis ANALYSIS_PATH` exactly once. Nonzero exit, `status: "CHANGED"`, or a manifest bound to a different analysis path means the PASS is stale: reset `loops.headline_replication.round` to 0 and re-fire step 6.5 on `ANALYSIS_PATH` before the next audit; step 6.5's all-analysis freshness gate then refreshes every sibling invalidated by the same code change. `status: "UNCHANGED"` permits direct audit re-fire. Never inspect a Git diff or decide whether a repair was "material." Any branch that can rebuild empirical data instead mandates step 6.5 unconditionally, because changed data bytes are outside this lightweight manifest.

7. **Empirics audit.** Set `ANALYSIS_ENTRYPOINTS` to every exact analysis entrypoint the empiricist ran to produce or update `ANALYSIS_PATH`, and set `RENDER_ENTRYPOINT` to the renderer bound by `RESULT_RECEIPT`. Launch `empirics-auditor` on `ANALYSIS_PATH` + `RESULT_BUNDLE` + `RESULT_RECEIPT` + `ANALYSIS_ENTRYPOINTS` + `RENDER_ENTRYPOINT` + the complete code surface + theory draft + its derived `VERIFY_RESULT_PATH`; set `AUDIT_OUTPUT_PATH = output/stage3a/empirics_audit.md` unless the caller supplied a versioned post-pipeline path. The auditor reads the replicator's per-claim results as confirmed/disputed signals (the replicator owns headline recomputation; the auditor does NOT re-derive headline numbers), runs the named entrypoints, verifies the receipt with re-rendering, checks that the renderer only presents declared bundle evidence, checks the complete execution surface for shared effects, checks methodology mechanics (clustering, robust SEs, sample construction), and reviews interpretation. Identification-design soundness is already gated at step 3; the empirics-auditor focuses on whether the code actually implements the audited plan.
<!-- DATA_FIRST_START -->
   In data-first mode also pass the exact `RIGHTS_INVENTORY`, `RELEASE_PATH`, `RELEASE_PLAN`, `RELEASE_BUNDLE`, `RELEASE_RECEIPT`, `RELEASE_ENTRYPOINT`, and `RELEASE_INPUT_PROVENANCE`. The auditor must verify the release receipt, inspect both sides of every declared rights/provenance mapping, and trace released columns back to the open inputs; a PASS over the analysis receipt alone is incomplete and routes as a missing audit artifact.
<!-- DATA_FIRST_END -->
   - If **PASS**: proceed to step 7.5 (data-integrity audit). Do not set the active-analysis pointer yet; downstream auditors still execute code.
   - If **FAIL**: below the 5-attempt audit-fix cap, use the fresh-attempt transition with the audit feedback and fix each flagged defect wherever it occurs, then reset `loops.headline_replication.round` to 0 and run step 6.5 on the new `ANALYSIS_PATH` with `FORCE_ALL_ANALYSES = true` before re-firing the auditor. At the cap, retire the pending receipt with the terminal audit report, then return to Stage 2 with those notes (the theory may be untestable as written).
7.5. **Post-empirics audit triad (data integrity + data selection + method canonical-availability).** `empirics-auditor` PASS verifies (a) the code runs and its with-cache output matches the report, AND (b) every stochastic LOAD-BEARING spec reproduces bit-for-bit when the derived cache is moved aside and the spec is re-run from scratch (issue #56 — same-path determinism, distinct from the headline-replicator's different-path tolerance check) — it does NOT verify the cache *content* is correct against the raw source (that is the data-integrity auditor's job below), nor whether the methods used are textbook-canonical or hand-rolled. Launch THREE auditors **in parallel** (single message, three Agent calls), supplying the current exact `ANALYSIS_PATH` to each: `data-integrity-auditor`, `data-selection-auditor`, and `method-checker`. The integrity auditor re-queries the source database and verifies cached field values against source (Class 1 — content); the selection auditor enumerates candidate identifiers under the documented inclusion rule with relaxed filters and diffs them against the cached universe (Class 2 — sample); the method-checker reads `.claude/skills/canonical-packages/SKILL.md` and flags every place the empiricist wrote custom code for an econometric method that has a canonical author-maintained package (Class 3 — method canonical-availability; see [issue #36](https://github.com/alejandroll10/zeropaper/issues/36)). Each returns PASS / REVISE / FAIL with severity-ranked named failure modes; outputs go to `output/stage3a/data_integrity_audit.md`, `output/stage3a/data_selection_audit.md`, and `output/stage3a/method_check.md` (+ summary JSON for method-checker). **Before aggregating, verify all three output files exist on disk** — `output/stage3a/data_integrity_audit.md`, `output/stage3a/data_selection_audit.md`, `output/stage3a/method_check.md`. If any is absent (typically because the corresponding agent crashed, hit a token limit, or timed out), re-launch only that absent agent and wait for its output before aggregating; do not infer a default verdict from an empty file. A silently-missing method_check.md treated as PASS-by-omission is exactly the issue-36 failure mode this triad exists to prevent. Then aggregate the three verdicts:
   - **All three PASS**: run `python3 -I -S code/utils/empirical_input_manifest.py check-all` one final time after all auditors have finished executing code. If its status is not `UNCHANGED`, code or verifier evidence changed during downstream verification: reset `loops.headline_replication.round`, re-enter step 6.5 on the current `ANALYSIS_PATH`, complete its all-analysis gate, and repeat step 7 plus the triad. Only a final `UNCHANGED` may run `results_pipeline.py activate --receipt "$RESULT_RECEIPT"` and then atomically set `pipeline_state.json:stage3a_theory_version = theory_version`, `stage3a_analysis_path = ANALYSIS_PATH`, and `stage3a_result_receipt = RESULT_RECEIPT`; the prior accepted triple stays unchanged until that pointer update succeeds. If this was a replacement, explicitly retire the prior receipt only after the pointer update. Then proceed to the **puzzle-triage entry check** (next section) and reset `pipeline_state.json:loops.data_integrity.round` AND `loops.method_check.round` to 0 (both counters tracked the triad's REVISE loops and are stale once the triad clears).
   - **Any data auditor REVISE (and no FAIL)**: increment `pipeline_state.json:loops.data_integrity.round`, then use the fresh-attempt transition with both data-audit reports; the new attempt repairs data construction, resets `loops.headline_replication.round`, passes step 6.5 with `FORCE_ALL_ANALYSES = true`, then repeats step 7 and all three auditors. The empirics-auditor re-fire does not consume its substantive-fix cap. At the 3-round data cap, route as FAIL rather than editing the candidate in place.
   - **method-checker REVISE only (data audits both PASS)**: increment `loops.method_check.round`, then use the fresh-attempt transition with `method_check.md`; the new attempt either uses the named canonical package or records the accepted (a)–(d) justification, then passes step 6.5 and step 7 before method-checker re-fires. At the 3-round method cap, route as FAIL.
   - **Both data REVISE and method-checker REVISE**: take the data-REVISE fresh-attempt route first and increment only `loops.data_integrity.round`; the rebuilt candidate repeats all three auditors. If method-checker remains REVISE on that new candidate, take the method-only fresh-attempt route and increment `loops.method_check.round` then.
   - **Any FAIL**: (a) for a source outage, retry preflight once; if still unreachable, retire the pending receipt with the outage diagnostic, set `status = "halted_data_audit_unreachable"`, and stop. (b) For substantive data FAIL, preserve the failing data counter, reset `loops.method_check.round`, and use the fresh-attempt transition to redesign/re-source/re-filter the data layer before step 6.5. (c) For method-checker FAIL, preserve its failing counter, reset `loops.data_integrity.round`, and use the fresh-attempt transition to rebuild the flagged sections with canonical packages before step 6.5. No FAIL branch rewrites the current receipt-bound analysis or code.
   - The triad re-fires on Stage 3a re-entry and after any requested cache regeneration. A cache is receipt-bound evidence: regeneration uses the fresh-attempt transition rather than changing the current candidate. Reset `loops.headline_replication.round`, require the new candidate to pass step 6.5 with `FORCE_ALL_ANALYSES = true` and step 7, then re-launch all three auditors. No stored PASS survives regeneration, and there is no mtime-based verdict replay shortcut.
<!-- DATA_FIRST_START -->
7.6. **Coverage audit and paired activation (data-first only — the triad becomes a quartet).** Launch `coverage-auditor` **in the same parallel batch** as the step-7.5 triad (single message, four Agent calls), supplying the exact `ANALYSIS_PATH`, binding dataset specification, `COVERAGE_CERTIFICATE_DECISION`, `COVERAGE_COMMITMENTS`, conditional exact certificate/digest, exact `RIGHTS_INVENTORY` and `RIGHTS_INVENTORY_SHA256`, reconciliation log paths from the plan's validation section, and cached event tables. In autonomous mode, the rights pair must resolve only from the current Gate-2 state pair; in manual mode, pass the caller-supplied authoritative pair. The auditor independently recomputes the rights file digest and compares the certificate's embedded binding to this independently supplied authority. It also re-does the triangulation on a stratified sample per event class — second-source independence, protocol execution over the claimed span, sample re-queries of the second source, reconciliation-log completeness and resolution spot-checks, waiver verification — and, when a certificate exists, completely re-enumerates the live authoritative universe before comparing certificate, live, and built key sets. It writes `output/stage3a/coverage_audit.md` with PASS / REVISE / FAIL and a per-class table. Under this mode, read every "all three PASS" / "three output files" instruction in step 7.5 as **four**: the existence check covers `coverage_audit.md` too (re-launch only the absent agent on a missing file), and no activation or state handoff occurs until coverage-auditor PASS; on clearance reset `loops.coverage_audit.round` to 0 alongside the others.

   After all four PASS, rerun `empirical_input_manifest.py check-all`, verify both `RESULT_RECEIPT` (with `--rerender`) and `RELEASE_RECEIPT`, and require the release plan/manifest to bind the current `theory_version`, exact `RIGHTS_INVENTORY`, exact `RELEASE_INPUT_PROVENANCE`, exact `RELEASE_PATH`, and exact `RELEASE_RECEIPT`. Run `results_pipeline.py activate-pair --analysis-receipt "$RESULT_RECEIPT" --release-receipt "$RELEASE_RECEIPT"`; ordinary `activate` is forbidden for either member while the pair is pending, and the trusted command moves both registry entries in one crash-recoverable transaction. Then, in one atomic state write, set `stage3a_theory_version = theory_version`, `stage3a_analysis_path = ANALYSIS_PATH`, `stage3a_result_receipt = RESULT_RECEIPT`, `dataset_release_path = RELEASE_PATH`, and `dataset_release_receipt = RELEASE_RECEIPT`. Only after that handoff atomically retire the superseded predecessor pair with `results_pipeline.py retire-pair --analysis-receipt "$OLD_RESULT_RECEIPT" --release-receipt "$OLD_RELEASE_RECEIPT" --reason "superseded dataset pair" --superseded-by-analysis "$RESULT_RECEIPT" --superseded-by-release "$RELEASE_RECEIPT"`. A crash before the state write leaves old pointers naming active evidence; a crash after it leaves new pointers naming active evidence. A stale/missing/invalid release re-fires the empiricist through the paired fresh-attempt transition before either receipt activates.
   - **Coverage REVISE (no FAIL anywhere):** increment `pipeline_state.json:loops.coverage_audit.round`, then use the fresh-attempt transition with the coverage report; the new attempt repairs the validation/build layer (runs the unexecuted span, reconciles unlogged discrepancies, re-documents unsupported resolutions, wires a named substitute second source, or restores certificate-governed rows when live source state still equals the certificate), resets `loops.headline_replication.round`, passes step 6.5 with `FORCE_ALL_ANALYSES = true`, then repeats step 7 and all four auditors. At the 3-round cap (`loops.coverage_audit.cap`), route as FAIL.
   - **Coverage REVISE combined with a data or method REVISE:** take the data-REVISE route first (it rebuilds the layer the coverage re-check depends on), incrementing only `loops.data_integrity.round`; the rebuilt candidate repeats all four auditors, and only a coverage REVISE that *persists* on the new candidate increments `loops.coverage_audit.round`.
   - **Coverage FAIL:** an event class is untriangulable as scoped (`mirror-triangulation` / `silent-single-source` with no accessible independent second source), the reconciliation log fabricated agreement (`unsupported-resolution` at severity 9+), the certificate binding is invalid, or a complete live re-enumeration proves substantive source/universe drift relative to a previously PASS certificate. These are **spec/evidence-level failures, not build fixes**: retire the pending receipt with the complete diagnostic and, in autonomous mode, atomically set both `dataset_spec_version = null` and `stage3a_theory_version = null` while preserving the accepted rights/certificate path-hash pairs and accepted analysis/release pointers as predecessor/supersession evidence, then return to Stage 2. Nulling the Stage-3a acceptance marker is mandatory even when an unchanged `theory_version` obtains a fresh certificate: that certificate may govern a different live key set, so the old build cannot satisfy Gate 4 by numeric version equality. The spec must rescope/add a waiver or the current unchanged spec must obtain a new census, and every applicable Gate-2 leg must pass before Stage 3a re-enters and produces a fresh cumulative replacement. A newly enumerated event after Gate 2 is a legitimate new fact, not a reason to weaken the check. A certificate-vs-build mismatch with unchanged live source state is REVISE above, never this branch. (An enumerator or second-source outage routes like the triad's source-outage FAIL: retry preflight once, then `status = "halted_data_audit_unreachable"` without nulling the scientifically accepted spec or Stage-3a acceptance marker.)
   - The coverage audit re-fires with the triad on Stage 3a re-entry and after any cache regeneration, under the same no-stored-PASS rule.
<!-- DATA_FIRST_END -->
8. Commit: `artifact: headline replication — {PASS/FAIL}; empirics audit — {PASS/FAIL}; data audits — {PASS/REVISE/FAIL × 2}; method check — {PASS/REVISE/FAIL}`

## Puzzle-triage entry check (mandatory after empirics-auditor PASS and both data audits PASS)

Before proceeding to Stage 4, you must check whether the empirical results contradict any prediction in `output/stage3/implications.md`.

1. Read `output/stage3/implications.md` and identify which implications were tested.
2. Read the exact `ANALYSIS_PATH` and the auditor's verification. On a versioned re-fire, this is the self-contained cumulative replacement report; use its declared prior-evidence inputs for lineage and do not assume the canonical first-attempt file exists.
3. For each tested implication: did the data contradict it (sign reversal, magnitude outside the predicted range, condition that should hold but failed)?
4. Write `output/stage3a/contradiction_check.md` with one of:
   - **NONE** — empirics confirm or are silent on every tested implication. Proceed to Stage 4.
   - **CONTRADICTIONS FOUND** — list the contradicted implications and what the data shows. **Proceed to puzzle triage** (`docs/stage_puzzle_triage.md`), not Stage 4.
5. Commit: `artifact: contradiction check — {NONE/CONTRADICTIONS FOUND}`

This step is mandatory and may not be skipped — silently jumping to Stage 4 after empirics PASS bypasses the puzzle-pivot mechanism that exists to extract value from theory-empirics disagreements.

## Re-fire on theory revision

Stage 3a is not one-shot. When the theory revises after the first 3a pass — Gate 3 INCREMENTAL rework, Gate 4 REVISE→Stage 2, Stage 6 Major Revision triggering theory-generator work, or any substantive content change — the original `empirical_analysis.md` becomes stale relative to the current theory. The empiricist must re-run on the revised content before Gate 4 can advance again.

**Trigger.** Any of:
- `stage3a_theory_version` is null or differs from `theory_version` (including after a theory-identity reset).
- `implications.md` is overwritten (Gate 3 INCREMENTAL rework, PIVOT, or any path that re-runs Stage 3) and the new file contains NOVEL or PUZZLE-CANDIDATE implications not present in the prior version.
- A referee identifies an empirical gap that wasn't addressed in the first 3a pass (e.g., "this prediction was deferred" or "the identification doesn't address X").
- Stage 6 Reject verdict triggers a deepen directive (see `docs/stage_6.md` Reject row): the deepen directive's empirics requirements become the focus of the re-fire.

**Procedure.**
Before step 1, establish K as the next unused serial for this output stem across the entire run; it never resets on a new theory or Regeneration. If the caller names an exact output stem, append `_aK` without overwriting it; otherwise set `ANALYSIS_PATH = output/stage3a/empirical_analysis_v{theory_version}_aK.md`. Set `ANALYSIS_BASELINE` to the exact retained baseline, derive `ANALYSIS_CONTRACT = output/analysis_specs/<ANALYSIS_PATH basename without .md>.contract.json` and `EXECUTION_SUMMARY = <ANALYSIS_PATH without .md>_execution.json`, and derive `RESULT_PLAN`, `RESULT_BUNDLE`, and `RESULT_RECEIPT` by replacing `.md` with `_results.plan.json`, `_results.json`, and `_results.receipt.json`; set `ANALYSIS_ENTRYPOINT = code/empirical_v{theory_version}_aK.py`, `RENDER_ENTRYPOINT = code/render_empirical_exhibits_v{theory_version}_aK.py`, and `INPUT_SNAPSHOT_DIR = output/stage3a/analysis_inputs_v{theory_version}_aK`. Copy every mutable document input into that fresh directory before writing `RESULT_PLAN`. Set `SUPERSEDES_ARGS` to one repeated `--supersedes <receipt>` pair for every prior active Stage 3a receipt the cumulative bundle absorbs. Never replace caller-supplied stems with defaults; supply all ten paths plus this exact array to every empiricist launch. Each substantive retry increments K, retires a failed pending receipt first, and allocates fresh code/input/artifact/exhibit paths.
<!-- DATA_FIRST_START -->
For data-first, derive the paired release paths for this same K exactly as step 5.5 specifies and set the release supersession to the active `pipeline_state.json:dataset_release_receipt` when non-null. Rebuild the release offline after the cumulative analysis run even when the changed fact does not alter the public schema: the manifest must bind the current theory version, exact build inputs, and current release code. Supply all release paths and the prior accepted release to the empiricist; never carry forward `dataset_release_path` by assumption.
<!-- DATA_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
0. **(empirical-first) Re-pass Gate 2 mechanism plausibility first.** A `theory_version` advance here means the mechanism changed. Before any empirical re-fire, confirm the Stage 2 Gate 2 mechanism-plausibility audit has been re-run on the revised mechanism and `pipeline_state.json:stage2_mechanism_version == theory_version` (see `docs/stage_2.md` "Gate 2: Mechanism Plausibility"). If it is stale, run that gate now — a revised channel that no longer delivers the documented sign/magnitude, or that contradicts the (possibly revised) identification design, should be caught before empirical effort is re-spent, exactly as on the first pass. Only then proceed to step 1.
<!-- EMPIRICAL_FIRST_END -->
<!-- DATA_FIRST_START -->
0. **(data-first) Re-pass Gate 2 first.** A `theory_version` advance here means the dataset specification changed. Before any build re-fire, confirm the Stage 2 spec audit and its conditional coverage census have re-run on the revised spec and `pipeline_state.json:dataset_spec_version == theory_version` (see `docs/stage_2.md` "Gate 2: Dataset Specification Audit"). If stale, run that gate now — a revised rule set, exact coverage promise, or fact portfolio that cannot survive every applicable Gate-2 leg should be caught before build effort is re-spent, exactly as on the first pass. Only then proceed to step 1.
<!-- DATA_FIRST_END -->
<!-- THEORY_FIRST_START -->
1. If the new content introduces a new causal claim or changes the theoretical object the empirics must identify, re-launch `identification-designer` (step 1 of the first-pass procedure) so the menu reflects the revised theory; otherwise carry forward the existing `identification_menu.md`.
<!-- THEORY_FIRST_END -->
<!-- DATA_FIRST_START -->
1. There is no identification step to refresh in data-first mode. The revised specification (already re-audited per step 0) and the re-derived fact portfolio are the binding inputs; proceed to step 2.
<!-- DATA_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
1. If the new content introduces a new causal claim or changes the theoretical object the empirics must identify, re-launch `identification-designer` (step 1 of the first-pass procedure) so the menu reflects the revised theory; otherwise carry forward the existing `identification_menu.md`.
   **Empirical-first re-fire launch.** The empirical-first `identification-designer` body has no hardcoded input/output defaults — it reads and writes the paths the launch instruction names — so a re-fire must supply them explicitly (a bare "step 1 of the first-pass procedure" pointer would leave the agent without file guidance). Launch with:

   > "You are operating under `--mode empirical-first` at a Stage 3a re-fire. Read the revised mechanism document `output/stage2/theory_draft_v{theory_version}.md`, `output/stage3/implications.md`, `output/data_inventory.md`, and the committed Stage 1 design `output/stage1/identification_design.md` (use it as your starting point and revise it to fit the new causal claim — do not design from scratch; write the result to the Stage 3a path below, not back to this file). The object to identify is the revised mechanism's new or changed causal claim. Save the revised committed design to `output/stage3a/identification_menu.md` (the canonical Stage 3a path downstream agents read; this supersedes the carried-forward design)."

   This mirrors the Stage 1 Step 4 launch (`docs/stage_1.md`); the only differences are the inputs (revised mechanism + the Stage 1 design instead of the idea files) and the output path (`identification_menu.md`, the Stage 3a canonical path, instead of `identification_design.md`). The single-design / question-match / no-theory-file framing lives in the body, not this instruction.
<!-- EMPIRICAL_FIRST_END -->

2. Launch `empiricist` with: the revised theory, the current `implications.md`, the exact prior accepted report at `pipeline_state.json:stage3a_analysis_path`, its bundle/receipt and still-needed artifacts, the (possibly refreshed) identification menu, and a focused instruction listing the specific new content to test. The empiricist updates `empirical_plan.md` (or writes an `empirical_plan_vN.md` for major changes) and plans one cumulative replacement that preserves every prior result still used by the paper alongside the new tests.
<!-- THEORY_FIRST_START -->
3. If a causal claim is in scope, run `identification-auditor` on the revised plan (same PASS/REVISE/FAIL loop as the first pass; same 3-revision cap). PASS gates execution.
<!-- THEORY_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->
3. If a causal claim is in scope, run `identification-auditor` on the revised plan (same PASS/REVISE/FAIL loop as the first pass; same 3-revision cap). PASS gates execution.
<!-- EMPIRICAL_FIRST_END -->
<!-- DATA_FIRST_START -->
3. There is no identification audit in data-first mode. The revised plan's gate is the step-4 orchestrator review (data-first variant: plan-vs-spec conformance, portfolio coverage, validation-plan completeness); the built dataset's gates are steps 6.5–7.6.
<!-- DATA_FIRST_END -->
4. Save the cumulative re-run to `ANALYSIS_PATH` and its evidence to `RESULT_PLAN` / `RESULT_BUNDLE` / `RESULT_RECEIPT`. **Do not overwrite the original `empirical_analysis.md` or its bundle.** Declare the prior accepted evidence as inputs, emit every still-valid old result plus every new/recomputed result into the fresh bundle, generate a complete fresh exhibit set, and pass the absorbed prior receipt(s) through `--supersedes`. Require `results_pipeline.py verify --receipt "$RESULT_RECEIPT" --rerender` to pass. A partial bundle that covers only the newly targeted claim cannot replace the Stage 3a pointer.
5. **Re-fire `headline-replicator` first on that exact `ANALYSIS_PATH`** (step 6.5 procedure; mechanically derives per-analysis verifier paths; resets `loops.headline_replication.round` to 0; same 3-round cap; same self-FAIL handling; same all-analysis freshness gate). The empiricist may have changed merge keys, sample windows, estimators, or the binding versioned headline; a stale per-analysis verifier from the prior pass is exactly the failure mode this step exists to prevent. Then run `empirics-auditor` on `ANALYSIS_PATH` + `RESULT_BUNDLE` + `RESULT_RECEIPT` + its fresh `VERIFY_RESULT_PATH` (saves to `output/stage3a/empirics_audit.md`; same audit-fix loop as the first pass; same 5-attempt cap). On auditor PASS, re-run step 7.5 (the data-integrity + data-selection + method-checker triad in parallel) on the regenerated cache and code — re-entry resets `loops.data_integrity.round` and `loops.method_check.round` to 0 and applies the same 3-revision caps to each. The triad's final `check-all` owns the one activation and atomic three-field handoff described there; no earlier verdict may set a Stage 3a pointer.
6. **Re-run the puzzle-triage entry check** (above) on the cumulative evidence — a new contradiction triggers puzzle-triage as it would on the first pass.
7. Confirm that step 7.5 already activated `RESULT_RECEIPT`, atomically handed off `stage3a_theory_version`, `stage3a_analysis_path`, and `stage3a_result_receipt`, and then retired every absorbed predecessor. Do not activate or hand off a second time after the contradiction check.
<!-- DATA_FIRST_START -->
   **Data-first override:** step 7.6, not 7.5, owns paired activation. Confirm it activated both `RESULT_RECEIPT` and `RELEASE_RECEIPT`, atomically handed off the analysis triple plus `dataset_release_path` / `dataset_release_receipt`, and retired both predecessor sets. Do not activate or hand off either receipt again after the contradiction check.
<!-- DATA_FIRST_END -->

**Gate 4 enforcement.** Before any Gate 4 advance, the orchestrator must verify `stage3a_theory_version == theory_version`. Stale empirics are a hard block, parallel to the `stage2b_theory_version` rule for theory-explorer.
<!-- DATA_FIRST_START -->
Under data-first, Gate 4 additionally requires non-null `dataset_release_path` / `dataset_release_receipt`, registry-active status for both the stored analysis and release receipts, `results_pipeline.py verify` exit zero with JSON `status: "PASS"` on both, and exact current-version/path bindings in the release plan and manifest. A valid analysis with an absent, stale, or prior-version release is a hard block.
<!-- DATA_FIRST_END -->

**Cap.** No hard cap on re-fires per problem — the constraint is the never-abandon rule plus the existing 10-round referee cap and, on unseeded runs, the 8-evaluation Gate 4 hard ceiling. On unseeded runs, re-fires that do not surface new evidence (auditor PASS with no new findings, no contradiction-check change) count toward Gate 4 plateau detection (see `docs/stage_4.md`). Seeded runs use Gate 4's correctness-only route instead.
