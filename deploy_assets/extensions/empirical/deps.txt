# Empirical extension Python deps.
# Single source of truth: read by scripts/apply_extension_empirical.sh (during
# setup) AND by update.sh's venv bootstrap, via `uv pip install -r`.
# Build-time only — installed into the project's .venv, not copied into deploys.
#
# Before adding a dep, check where it writes caches/config by default (#212):
# a `~/.something` default outside the sandbox writable sets (Claude
# settings.json allowWrite, codex writable_roots in launch.sh, grok
# sandbox.toml read_write) fails with PermissionError on first use — silently
# absent from any test that skips that code path. Prefer pointing the library
# inside the project via env var/API (the EDGAR_LOCAL_DATA_DIR pattern, #210)
# over widening three sandbox configs. Declared ~/.cache subdirectories and
# ~/.matplotlib are already writable in all confining runtimes; audited
# 2026-07-31: wrds (interactive-
# only .pgpass), gdown (~/.cache/gdown, allowed), openassetpricing (in-memory
# BytesIO), fredapi/pandas-datareader/requests (no persistent cache).
pandas
numpy
polars
pyarrow
statsmodels
linearmodels
scipy
# Direct module-scope import in utils/edgar_utils.py — declared explicitly so a
# transitive dep dropping it (wrds/fredapi pull it in today) can't break EDGAR.
requests
fredapi
pandas-datareader
# wrds.Connection.connect() contains hidden retries. The server deliberately
# invokes 3.5.0's private, one-attempt engine primitive and regression-tests
# its implementation; do not upgrade without re-auditing that contract.
wrds==3.5.0
edgartools
openassetpricing
gdown
python-dotenv
