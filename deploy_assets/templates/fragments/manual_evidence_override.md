<!-- MANUAL_START -->
**Manual-source override.** This project has no autonomous `pipeline_state.json` contract and no fixed Stage 2b/3a/3b namespace. Use the paper, the caller-supplied research materials, and every active receipt in `process_log/results_registry.json` as the complete source map. Follow each receipt's declared reports, code, artifacts, and rendered exhibits wherever they live under `output/`; do not guess canonical stage paths. Any later instruction in this body that assumes an autonomous stage, state pointer, or fixed stage directory is inapplicable in manual mode. If a source needed for a check is neither supplied by the caller nor declared by an active receipt, mark that check N/A or request the missing source instead of inventing a path.
<!-- MANUAL_END -->
