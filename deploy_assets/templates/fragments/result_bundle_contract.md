### Paper-facing computation contract

Every computed result that could support the paper belongs in one canonical result bundle. Raw or high-dimensional outputs may stay in CSV, Parquet, JSONL, NetCDF, model checkpoints, or another natural format, but the bundle must declare and describe each such artifact. Formal theory objects — theorem numbering, proof constants, normalizations, and symbolic conditions — are not result records unless a script computes a paper-facing numerical implication from them.

The analysis entrypoint writes the bundle; it does not write final tables or figures. Its caller-supplied prose report path must appear in the run plan's `artifacts` array and as the matching described bundle artifact, and the analysis must freshly create it—an undeclared report stays inside the isolated workspace and is intentionally discarded. A separate renderer then reads only that bundle and its `renderer.inputs` subset of declared artifacts and produces every result-bearing LaTeX table and PDF/PNG figure. When `renderer.inputs` is omitted, ordinary legacy runs expose all declared artifacts; new work should state the smallest sufficient subset explicitly. The renderer may format, select declared rows, and plot declared data; it must not read raw inputs, refit a model, query a service, or carry hard-coded numerical cells/series. This separation is binding: the saved result bundle plus its renderer-safe artifact subset must be sufficient to recreate the exhibits.

Before analysis, write the supplied `RESULT_PLAN` as a run-plan-v1 JSON object listing the exact `producer_code`, `producer_inputs`, `artifacts`, `renderer_code`, `renderer_inputs`, and `exhibits` paths the run will use. Keep every result-owned plan, bundle, receipt, artifact, and exhibit outside the audit-only `output/evidence/` namespace. Also set `provider_credentials` to the smallest required array chosen from `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `UF_API_KEY`, `DEEPINFRA_TOKEN`, and `LOCAL_LLM_API_KEY`; use `[]` unless this producer actually calls that provider. The trusted runner imports only those selected keys from the exported environment or project `.env`; unselected provider keys never reach the process, `.env` is never mounted, and renderers receive none. First copy every mutable pipeline document the computation reads—plans, implications, audit feedback, current reports—into the fresh attempt's `INPUT_SNAPSHOT_DIR`; the analysis reads and declares those immutable copies, never the canonical file that a later retry may revise. Stable raw datasets may remain at their original paths. Run both entrypoints through `code/utils/results_pipeline/results_pipeline.py` using the exact paths specified below. The analysis process receives `RESULTS_BUNDLE_PATH`; the renderer receives that bundle path plus an absolute `RESULTS_EXHIBIT_ROOT` staging directory and must create each declared exhibit at `Path(RESULTS_EXHIBIT_ROOT) / <project-relative-exhibit-path>`. Pass the prose report path as an explicit analysis-command argument. The utility snapshots plan/code/inputs before execution, requires the resulting bundle to match the plan exactly, records the actual commands, and hashes all declared evidence. Never hand-edit a rendered exhibit after its receipt is written.

Treat every active receipt's plan, declared code, inputs, outputs, recorded command, replacement relation, and exhibit bytes as immutable evidence. A re-fire uses fresh versioned plan, bundle, artifact, exhibit, and receipt paths; prefer versioned entrypoints too, and never overwrite an active or failed attempt's owned files. If genuinely shared code or a declared input changes, a fresh `run --supersedes <old-receipt>` may proceed only when that shared source is the old receipt's sole staleness; replace every affected active receipt before paper audit. The registry keeps the old run recorded while the replacement is rendered, verified, and substantively reviewed, and the new receipt preserves the exact `supersedes` relation after activation. Explicit `activate --receipt <new-receipt>` adds the accepted receipt without removing the old one.
<!-- AUTONOMOUS_START -->
This makes the autonomous cross-file handoff crash-safe: atomically update the stage report/receipt pointers to the new active receipt, then explicitly run `retire --receipt <old-receipt> --reason "superseded by <new-receipt>" --superseded-by <new-receipt>`.
<!-- AUTONOMOUS_END -->
<!-- MANUAL_START -->
Manual mode has no stage pointer. Activation in `process_log/results_registry.json` is the durable handoff: report the accepted active receipt and its declared report path to the caller, then explicitly run `retire --receipt <old-receipt> --reason "superseded by <new-receipt>" --superseded-by <new-receipt>`. Never create `pipeline_state.json` or a stage-named pointer for this handoff.
<!-- MANUAL_END -->
Paper audit fails closed while an activated replacement and its declared predecessor are both active. If both runs remain active for combined coverage, omit `--supersedes` and retain both sets of byte-stable, disjoint evidence. A failed pending attempt must be explicitly retired with a non-empty reason before allocating another fresh attempt; never reuse its paths. Presentation-only renderer changes also use a fresh attempt; an active `render` invocation accepts only its exact recorded command and exact recorded bytes. Analysis and rendering run in default-deny workspaces containing only their declared project sources, a read-only neutral mount of the project venv/base interpreter, and fixed system runtime files; undeclared live-project paths are absent, stdin is EOF, and staged outputs must stay under `output/`. Producers receive only an allowlisted environment; provider-backed producers may still receive plan-selected API credentials and outbound network, while renderers receive neither provider credentials nor network environment. Only the trusted parent holds the lifecycle lock and publishes validated outputs. A separate lock-free supervisor kills and waits for the ordinary sandboxed process group if that parent dies; Linux payloads also run as PID 1 in a private namespace so normal exit cannot orphan background descendants. This bounds each execution to its own declared inputs instead of copying every historical receipt and prevents a failed child from changing lifecycle bytes or releasing serialization. Every non-empty provider/proxy credential is forbidden in recorded command arguments; literal selected-provider credentials and percent-decoded proxy passwords of at least eight bytes are also rejected from staged files. Deliberate encoded/network exfiltration by a networked producer, hostile detached macOS grandchildren, and non-reflink large-input copies remain the explicitly tracked #267–#269 boundaries. Prose reports may interpret results for downstream agents, but they are not numerical sources of truth.

In command examples, uppercase path names denote values supplied in the launch prompt. Substitute their exact literal paths before execution; do not rely on undeclared shell variables.

The published JSON Schemas are structural preflight aids, not the full contract. Only the appropriate trusted command—`results_pipeline.py run` for ordinary computation or `run-empirical` for empirical evidence—applies normalized-path, plan/bundle equality, cross-reference, duplicate-ownership, freshness, and lifecycle checks. Schema acceptance never authorizes a run the utility rejects.

Use run-plan version 1 from `code/utils/results_pipeline/run-plan-v1.schema.json` and bundle schema version 1 from `code/utils/results_pipeline/results-v1.schema.json`. Result IDs are stable lowercase semantic identifiers. Each result has a non-empty `description` and a `value`, a declared `artifact`, or both; `unit`, `uncertainty`, `display`, `selector`, and free-form object metadata are optional. Do not invent a discipline taxonomy. Each exhibit declares the result IDs it displays. Use `producer.reproducibility = captured` by default. Ordinary result-receipt v2 and empirical result-receipt v3 both bind the trusted runner's execution-time environment capture for the producer and renderer: resolved launcher bytes, host/runtime identity, numerical/locale flags, dependency manifests, and installed project-venv distribution metadata; the runner rejects an environment change between the pre- and post-execution captures. Empirical v3 additionally binds scientific lineage. This is historical provenance, not an imported-module/native-library closure. Use `exact` only when a complete content-addressed environment/lock manifest is itself a declared producer input and the auditor can validate that it covers the interpreter, imported packages/native libraries, and runtime flags; exact multi-language closure remains tracked in #271. Use `bounded` only with a separately audited nondeterminism bound. The label is a producer assertion, not a mechanical verifier verdict; the evidence audit must reject `exact` or `bounded` unless the declared manifest or bound actually supports it.
