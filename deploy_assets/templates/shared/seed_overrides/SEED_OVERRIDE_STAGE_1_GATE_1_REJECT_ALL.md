### Seeded-mode override (applies because `seeded: true` in `pipeline_state.json`)

**REJECT ALL is not allowed in seeded mode** — the orchestrator may NOT exit to Stage 0. Re-run idea-generator with feedback, or escalate to picking the highest-scored idea regardless of the reviewer's overall verdict. The seed's problem is fixed; only the mathematical formulation is being chosen here.
