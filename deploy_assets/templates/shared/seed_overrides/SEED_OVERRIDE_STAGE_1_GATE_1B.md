### Seeded-mode override (applies because `seeded: true` in `pipeline_state.json`)

<!-- DATA_FIRST_START -->
**Data-first reading of this override.** "Execution/proof depth" reads as **construction/validation depth**: even a KNOWN-flagged dataset idea can carry its contribution in complete triangulated coverage, corrected dating, and open release of what incumbents keep closed. Proceed and log the concern exactly as written.
<!-- DATA_FIRST_END -->
**Gate 1b KNOWN does NOT kill the seed.** Report the concern to `output/seed/novelty_concern.md` and **proceed to Gate 1c**. The contribution may live in execution/proof depth even if the core idea is known. Do not iterate Stage 1 for a new idea.

**Gate 1b INCREMENTAL**: also proceed (not halt). Log the flag to `output/seed/novelty_concern.md` so paper-writer positions the contribution carefully.

**Gate 1b NOVEL**: proceed as normal.
