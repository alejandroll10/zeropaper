### Seeded-mode override (applies because `seeded: true` in `pipeline_state.json`)

The aggregate score and `ADVANCE / REVISE / MAJOR REWORK / ABANDON` label are diagnostic in seeded mode; they do not route the run. The seed fixed the research direction, and the preceding math/design/empirical gates already own correctness.

- Read `output/stage4/triage_vN.md` as well as both scorer reports. If the triage contains a `[FIX]` item that identifies a **specific correctness or rigor defect**, or either scorer identifies such a defect, return that claim to its existing owning audit. Forward only the defect and its cited evidence; neither triage nor scorer assertion overrules the audit. A strategic, framing, novelty, importance, scope, fertility, or format suggestion does not become a correctness defect merely because triager assigned `[FIX]`. Follow the owning gate's normal seeded/faithful repair-or-halt route.
- Otherwise, proceed to Stage 5. Record KNOWN/INCREMENTAL evidence and any seed-pinned importance, novelty, surprise, fertility, scope, or format ceiling in `output/seed/limitations.md`. Do not forward strategic scorer/triage suggestions, wait for a numeric plateau, or launch deepening to raise dimensions the fixed seed controls.

If `faithful == true`, the existing Gate 4 → Stage 5 `faithful-drift-auditor` remains binding. Before advancing, launch it with explicit inputs `output/seed/mechanism_contract.md`, the current `output/stage2/theory_draft_vN.md` selected by `pipeline_state.json:theory_version`, and every applicable current contribution-bearing evidence result: `output/stage3a/empirical_analysis.md` plus current versioned `empirical_analysis_v*.md` files, and/or `output/stage3b/experiment_results.md` plus current versioned `experiment_results_v*.md` files. Require `NO-DRIFT` and re-audit after any remedy. For unauthorized contribution/headline/mechanism drift, restore the contract framing. For a verified theoretical, empirical, or experimental result that differs from a seed-stated quantity, **never alter the finding to match the seed**: preserve the actual result, document the discrepancy and honest framing in `output/seed/limitations.md`, and then re-audit. Do not give the auditor a scorer or audit report as the contribution artifact. Plain `--seed` has no extracted mechanism contract and therefore does not run this audit.

Forward presentation notes to the Stage 5 paper-writer as usual. Continue at step 12; the unseeded branch-manager and score-escalation path below does not run.
