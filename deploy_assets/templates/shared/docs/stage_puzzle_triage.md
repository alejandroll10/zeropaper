# Stage: Puzzle Triage

**Agent:** `puzzle-triager`

**Fires when:** (a) the exact empirical report at `pipeline_state.json:stage3a_analysis_path` or exact experimental report at `pipeline_state.json:stage3b_results_path` **contradicts** at least one prediction in `output/stage3/implications.md`, OR (b) Stage 3 tagged at least one implication **PUZZLE-CANDIDATE** — gap-scout reports the literature shows a sign reversal or order-of-magnitude discrepancy vs. the prediction (the lit-check report is the contradiction evidence; fire before any formal empirics). If results confirm the theory or are silent on its predictions, **skip this stage** — proceed directly to Stage 4.

This stage operationalizes the principle that surprises are discoveries: when a well-grounded theory predicts X and well-measured data shows not-X, the contradiction itself is often the most valuable contribution. The triager decides whether to pivot the paper around that puzzle, fix the empirics, restrict the theory's scope, abandon the idea, or ship an honest null.

**Artifact guard.** A contradiction is not a puzzle-pivot opportunity if the best explanation is a data-construction, proxy, sample, merge-key, aggregation-key, standard-error, coding, or identification-design artifact. That is an empirical/debugging failure even when the artifact is interesting. Route to `FIX-EMPIRICS` if the artifact can be corrected and the original prediction retested; route to `BACK-TO-IDEA` before Stage 5 if the corrected design removes the paper's identifying variation; route to `HONEST-NULL` after Stage 5. Do not promote a methodological warning to the main paper unless the operator explicitly asked for methods-note outputs, an external replication shows the artifact changes published conclusions, or the contribution is a formal methodological result — a stated theorem with proof, applicable beyond this paper's specific analysis or dataset (e.g., a new estimator's consistency, an identification theorem) — not a simulation rejection rate, placebo battery, or debugging insight even framed as a general claim.
<!-- DATA_FIRST_START -->
**Data-first mode note.** In this mode the "theory" is the dataset specification, the "predictions" are the fact portfolio's expectations, and the canonical trigger is a **failed replication target** — a known published result that did not reproduce on the new data. The `puzzle-triager` body is mode-overlaid with the data-first decision tree (side-by-side construction isolation as the gating evidence for PIVOT; all five verdicts live with redefined semantics). Apply these substitutions to the generic instructions below:

- **Gate 2 references:** Gate 2 is the **dataset-specification audit plus its conditional coverage census** (`mechanism-auditor` then, when REQUIRED, `empiricist` in census-only mode — see `docs/stage_2.md`), NOT a math audit; it is never skipped, and `dataset_spec_version = theory_version` is set only after every applicable leg passes. Where the triager's input list mentions "math audit results," substitute the spec audits (`output/stage2/mechanism_audit_v*.md`), any accepted `output/stage2/coverage_certificate_c*_s*_v*.json`, the Stage 3a audit chain (`output/stage3a/{empirics,data_integrity,data_selection,coverage}_audit.md` as they exist), the most recent `referee-mechanism` report (Stage 6, if it exists), and any prior `output/stage4/self_attack_v*.md`. Stage 2b is permanently skipped; `stage2b_theory_version` resets are harmless no-ops. Every generic acceptance-version reset below—including RECONCILE and Pivot step 1—**also sets `dataset_spec_version = null` in the same commit** while preserving the run-global `dataset_spec_serial` / `dataset_coverage_certificate_serial`, accepted `dataset_rights_inventory` / `dataset_rights_inventory_sha256`, and conditional `dataset_coverage_certificate` / `dataset_coverage_certificate_sha256` bindings. This additional reset is load-bearing.
- **FIX-EMPIRICS branch (a):** there is no identification-audit re-entry — the identification agents do not exist in this mode. Classify the triager's notes instead as: (i) a build/computation defect on our side → re-launch `empiricist` at the failing computation per `docs/stage_3a_empirical.md`; or (ii) the **missing side-by-side isolation** (the triager's data-first tree routes here when the construction difference is candidate-named but unexamined) → re-launch `empiricist` with the specific instruction to recompute the failed statistic under the prior paper's convention on this dataset, as a portfolio exhibit. Loop-counter handling is as written (including `loops.coverage_audit` starting at 0 alongside `loops.data_integrity` on a cache-rebuilding re-run).
- **RECONCILE:** the mutate instruction becomes: "Add an explicit construction-scope statement to the spec's fact-portfolio plan: the published result holds under the prior convention, this dataset's result under ours, and the paper reports both with the convention difference stated." The re-run list applies with the data-first Gate 2 (spec audit) and Stage 2b skipped.
- **HONEST-NULL:** a failed replication whose audit chain passed is legitimate content — document it in the paper's **validation section** as a non-replication with the construction analysis as far as it got (the limitations section carries only the residual uncertainty).
- **Pivot sequence:** step 2's pivot object is the **fact portfolio, not the dataset**: `theory-generator` (pivot strategy, dataset-spec mode) rewrites the spec around the promoted adjudication — the headline becomes "the published fact is an artifact of a named construction difference," with the side-by-side isolation as the primary exhibit. The dataset schema and caches carry forward; the Stage 3a re-fire ("Re-fire on theory revision") recomputes the portfolio exhibits by cumulative replacement, not a from-scratch rebuild. Step 3's Gate 2 re-run is the spec audit plus any REQUIRED census (`dataset_spec_version` moves only after both pass). Step 5 (Stage 2b) is skipped. Step 7's resolution check reads: the pivoted portfolio's exhibits must contain the side-by-side isolation reproducing the disagreement — that is what "explains the contradiction" means here.
<!-- DATA_FIRST_END -->
<!-- EMPIRICAL_FIRST_START -->

**Empirical-first mode note.** Several instructions below reference Gate 2 (math audit), Stage 2b (theory exploration), and `output/stage2/math_audit_v*.md` / `output/stage2/freeform_audit_v*.md` files. **The *math-audit* form of Gate 2, Stage 2b, and the math-audit files are no-ops under empirical-first mode** — they are permanently skipped (see `docs/stage_2.md`) and the math-audit files do not exist. **But under empirical-first Gate 2 IS the mechanism-plausibility gate (`mechanism-auditor`), and it is NOT skipped** — it must re-run on any revised mechanism, exactly as the math audit does in theory-first. So when a RECONCILE or PIVOT verdict says "re-run Gate 2 + Stage 2b," in empirical-first mode re-fire the **mechanism-plausibility Gate 2** (resetting `stage2_mechanism_version` per the Pivot sequence), Gate 3 (novelty), Stage 3 (implications), and Stage 3a (empirical analysis re-fire per `docs/stage_3a_empirical.md` "Re-fire on theory revision"); skip only Stage 2b. The `stage2b_theory_version` resets remain harmless no-ops (the field stays `null`); the `stage2_mechanism_version` reset on PIVOT is **load-bearing**, not a no-op (see the Pivot sequence step 1). When the triager's input list mentions "math audit results," substitute the most recent `referee-mechanism` report (Stage 6, if it exists) and the prior `output/stage4/self_attack_v*.md` — these are the analogous content-quality records under empirical-first.
<!-- EMPIRICAL_FIRST_END -->
<!-- MEASUREMENT_FIRST_START -->

**Measurement-first mode note.** Several instructions below reference Gate 2 (math audit) and Stage 2b (theory exploration). Under measurement-first, Gate 2 is **two-halved** (see `docs/stage_2.md`): the plan-time **design gate** (`experiment-reviewer` on the measurement plan, tracked by `stage2_design_version`) and the **deferred math audits** (fired on the post-Stage-3b characterization). Stage 2b is permanently skipped. So when a RECONCILE or PIVOT verdict says "re-run Gate 2 + Stage 2b": re-fire `theory-generator` (construct mode) on the revised construct spec, re-pass the **design gate** (re-setting `stage2_design_version`), then Gate 3 (novelty), Stage 3 (implications), Stage 3b (experiment re-fire), the **post-experiment characterization + deferred math audits** per `docs/stage_3b_experiments.md` — and skip only Stage 2b. The `stage2b_theory_version` resets remain harmless no-ops (the field stays `null`); the `stage2_design_version` reset on PIVOT is **load-bearing**, not a no-op (see the Pivot sequence step 1). A puzzle-triage contradiction in this mode arrives *before* any characterization exists (the experiments are the evidence core) — the pre-pivot construct spec has no math-audit files, so where the triager's input list mentions "math audit results," substitute the design review (`output/stage2/design_review_v*.md`) and the prior `output/stage4/self_attack_v*.md` if any.
<!-- MEASUREMENT_FIRST_END -->

## Entry check

Before launching the triager, verify the contradiction is real:

1. Read `output/stage3/implications.md` and identify which implications were tested.
2. Read every result file that exists:
   - the exact report at `pipeline_state.json:stage3a_analysis_path` (if `--ext empirical`)
   - the exact report at `pipeline_state.json:stage3b_results_path` and the exhibits bound by `stage3b_result_receipt` (if `--ext theory_llm`)
   Both may exist when both extensions are enabled — use all of them.
3. For each tested implication: did the data contradict it (sign reversal, magnitude mismatch outside predicted range)?
4. Check whether the contradiction is attributable to a data/proxy/sample/merge-key/aggregation-key/SE/coding/identification artifact. If yes, pass that diagnosis prominently to the triager and apply the artifact guard above; the default path is `FIX-EMPIRICS`, not `PIVOT`.
5. If no contradiction across any result source: **skip this stage**, proceed to Stage 4.
6. If at least one tested implication was contradicted by at least one result source: launch the puzzle-triager.

## Procedure

1. Read `pipeline_state.json` for current `loops.pivot.round` (default 0).
2. Launch `puzzle-triager` with: theory draft, `implications.md`, **every contradiction-evidence file** (post-empirics path: the exact current empirical analysis and/or the exact experiment report at `pipeline_state.json:stage3b_results_path` that contradicted; Stage-3 PUZZLE-CANDIDATE path: the `output/stage3/lit_check_impl_N.md` reports for each PUZZLE-CANDIDATE implication), literature map, math audit results, pipeline state. If multiple evidence sources disagree (e.g., empirics confirm but experiments contradict, or lit-check contradicts but empirics confirm), note the disagreement in the input — the triager should flag this in its rationale and typically recommend FIX-EMPIRICS rather than PIVOT until the measurement conflict is resolved.
3. Triager saves to `output/puzzle_triage/triage_pN.md` where N = `loops.pivot.round + 1`.
4. **(Stage-3 lit-check trigger only)** Orchestrator appends to `pipeline_state.json:triaged_lit_implications` an entry `{implication_key, verdict, triage_file}` for each PUZZLE-CANDIDATE implication just triaged. **Verdict translation:** the triager emits raw `FIX-EMPIRICS`; on the Stage-3 lit-check trigger the orchestrator records this as `"FIX-EMPIRICS-b"` (the no-empirics branch — see the FIX-EMPIRICS verdict-table row), not the raw string. All other verdicts pass through unchanged. See "Re-fire guard" below for the canonicalization rule and the verdict semantics (FIX-EMPIRICS-b blocks re-fire; RECONCILE removes this implication's entry; BACK-TO-IDEA and PIVOT reset the whole list to `[]`). Skip this step for the post-empirics trigger — that path uses `loops.pivot.round` / `pivot_resolved` instead.
5. Commit: `artifact: puzzle triage round {N+1} — {VERDICT}`.

## Acting on the verdict

| Verdict | Pipeline action |
|---------|----------------|
| **NORMAL-PROCEED** | (Triager flags an inconsistency — empirics did not actually contradict.) Proceed to Stage 4 and review the entry check. |
| **PROBE-NULL** | The contradicted spec was tagged `[ROLE: STRENGTHENING-PROBE]` in the exact current empirical analysis (or, for theory_llm contradictions, in `output/stage3b/experiment_design.md` and the exact report at `pipeline_state.json:stage3b_results_path`) — an optional spec whose negative result does not move the headline (see the spec-role schema in `empiricist.md` / `experiment-designer.md`). Append a short note to `process_log/run_log.md` (or equivalent) recording "probe null — baseline intact" with the spec name and triage-file path, then proceed to Stage 4. **Do not increment `loops.pivot.round` or `loops.fix_empirics.round`. Do not reset `loops.data_integrity.round` or `loops.headline_replication.round`** — this is exception 2 (positive non-reset) to the generic Audit-loop scoping rule: baseline analysis is intact and no re-run is occurring, so no artifact changed and no loop zeroes. Do not increment `theory_version`. The triage file (`output/puzzle_triage/triage_pN.md` with verdict PROBE-NULL) remains on disk for transparency but does **not** count as a "PUZZLE-CANDIDATE confirmed by empirics" for downstream scorer Surprise-floor (`scorer-core.md`) or paper-writer puzzle-framing (`paper-writer.md`) rules — those gate on the triager's measurement-quality verdict on a non-PROBE-NULL outcome. |
| **FIX-EMPIRICS** | Two branches: <br/>**(a) Post-empirics trigger (empiricist exists):** run the data-source preflight from `docs/stage_3a_empirical.md` ("Preflight: data-source liveness") — a long puzzle-triage / Stage-4 gap may have outlived the WRDS session. Then **classify the triager's notes**: if the contradiction is attributed to a misspecified identification strategy (wrong design class, mis-stated estimand, missing identifying assumption — concerns the `identification-auditor` would name) rather than a proxy or measurement issue, re-enter at `empirical_plan.md` revision and re-run **step 3 of `docs/stage_3a_empirical.md` (identification audit)** before re-execution — this re-entry is a fresh identification-audit cycle on revised content, so per the generic Audit-loop scoping rule `loops.identification_plan_revision.round` starts at 0 (verify it is 0 and reset it explicitly if a stale value carried over from a prior audit cycle). Otherwise (proxy, measurement, sample, or estimation-mechanics issue) re-launch `empiricist` directly at `empirical_analysis`. Either way, theory is unchanged. The re-run rebuilds the cache (re-firing step 7.5 fresh) and produces a new headline that step 6.5's replicator must re-verify, so `loops.data_integrity`, `loops.method_check`, and `loops.headline_replication` all start at 0 per the generic rule (a stale counter would otherwise force-FAIL the next legitimate REVISE or premature-cap the first legitimate disagreement on the new artifacts). Do not increment `loops.pivot.round`. **Cap (this branch only): `loops.fix_empirics.cap` FIX-EMPIRICS rounds per puzzle.** Increment `loops.fix_empirics.round` on each FIX-EMPIRICS verdict in this branch only. On reaching the cap (`loops.fix_empirics.round >= loops.fix_empirics.cap`), force escalation to RECONCILE (if scope-restrictable) or HONEST-NULL (otherwise). <br/>Every `empiricist` re-launch on this branch goes through Stage 3a's fresh-attempt transition like any other, including its `loops.unowned_failure` responsibility test; the loops named below reset per the generic rule, but `audit_fix` and `unowned_failure` are exception-3 loops that do **not**, and this branch leaves `theory_version` unchanged, so nothing else zeroes them here either. <br/>**(b) Stage-3 lit-check trigger with no `--ext empirical`:** the lit evidence is rated DEBATABLE; there is no empiricist to re-run. Orchestrator leaves the PUZZLE-CANDIDATE tag in place (no re-tag, no information loss) and proceeds to Stage 4. Downstream agents (scorer, paper-writer) read `output/puzzle_triage/triage_pN.md` and gate the Surprise-floor / puzzle-framing rules on measurement-quality being STANDARD — DEBATABLE means those rules do not fire. Do not loop the triager; the cap and counter in branch (a) do not apply here. |
| **RECONCILE** | Launch `theory-generator` in `mutate` mode with instruction: "Add an explicit scope condition stating where the result holds. Empirical results show data sits outside that scope." **Increment `theory_version` and set every present Stage-2/3 acceptance-version field to `null`** (`stage2b_theory_version`, `stage2_mechanism_version`, `stage2_design_version`, `stage3a_theory_version`, `stage3b_theory_version`), retaining accepted report/receipt pointers for cumulative replacement. The reconciled theory is a fresh theory version, so those theory- and empirical-scoped audit loops (`fix_empirics`, `data_integrity`, `method_check`, `headline_replication`, `evidence`) start at 0 per the generic Audit-loop scoping rule. The `core.md` exception-3 loops are deliberately not in that set and are not silent omissions: this is a version bump, **not** a fresh theory identity, so `unowned_failure` correctly does **not** reset here — that non-reset is what stops its cap from being self-feeding — while `audit_fix` is cleared by Stage 3a's own "Re-fire on theory revision" entry, which the null `stage3a_theory_version` set above makes unavoidable on the next Stage 3a run. Re-run Gate 2 (math audit), Gate 3 (novelty), Stage 2b (exploration), Stage 3 (implications), AND Stage 3a (full empirical analysis, if `--ext empirical`) / Stage 3b (experiments, if `--ext theory_llm`) on the revised theory — the scope condition is a structural change and all downstream artifacts are stale. Do not increment `loops.pivot.round`. **Remove this implication's entry from `triaged_lit_implications`** so the re-fire guard does not block re-triage of the same prediction in the mutated theory. |
| **BACK-TO-IDEA** | Return to Stage 1 with the triager report as input to the idea-reviewer. **Do NOT increment `problem_attempt`** (the problem is unchanged — only the idea is being replaced). Before re-entry, increment `theory_attempt`, reset `theory_version` to 1, and apply the mandatory fresh-theory identity reset from `core.md`; then set `current_stage: "stage_1"`. Before re-running idea-reviewer, apply `docs/stage_1.md` step 2 re-entry logic: prefer a pre-screened runner-up from `pipeline_state.json:stage1_candidates` (entry with `eliminated: false AND winner: false AND prototype == TRACTABLE`) over regenerating. Skip if Stage 5 has begun (paper exists) — use HONEST-NULL instead. **Reset `triaged_lit_implications` to `[]`** — new idea, fresh implication space. |
| **PIVOT** | Run the **pivot sequence** documented below. **Reset `triaged_lit_implications` to `[]`** as part of step 1 of the pivot sequence — new theory, new implication space. |
| **HONEST-NULL** | Set `pivot_resolved: false` in pipeline state (see State updates). Then two paths: (a) if Stage 5 has begun, document the failed prediction in the limitations section, set `current_stage: "stage_5"` and proceed; (b) if no paper exists yet, set `current_stage: "stage_0"` and return to Stage 0 with the failure notes (also increment `problem_attempt`). Do not pivot a third time. |

{{SEED_OVERRIDE_STAGE_PUZZLE_TRIAGE}}

## Pivot sequence (when verdict is PIVOT)

A pivot is a full theory revision. The new theory needs new implications, lit-checks, and validation — not just empirical re-run. Follow this sequence end-to-end:

1. **Update state.** Increment `loops.pivot.round`, increment `theory_attempt`, reset `theory_version` to 1, and apply the mandatory fresh-theory identity reset from `core.md`: before the state update retire active feasibility evidence for the abandoned identity, then set every present acceptance-version field to `null` (`stage2b_theory_version`, `stage2_mechanism_version`, `stage2_design_version`, `stage3a_theory_version`, `stage3b_theory_version`) while retaining accepted report/receipt pointers for cumulative replacement. Also reset `triaged_lit_implications` to `[]`. The pivoted theory is a new identity, so the theory- and empirical-scoped audit loops (`fix_empirics`, `data_integrity`, `method_check`, `headline_replication`, `evidence`) start at 0 per the generic Audit-loop scoping rule, and the `core.md` reset invoked above separately zeroes the exception-3 loops that rule deliberately does not reach — `audit_fix`, plus `unowned_failure` under `--ext empirical`. Take those from `core.md` rather than from any list here. `loops.pivot` is the one counter that must **survive**: it is incremented in this same step and it bounds pivots per problem, so this is deliberately *not* the blanket every-loop reset the canonical Regeneration entry procedure performs, and replacing it with one would silently uncap pivoting. These non-loop acceptance fields must all be null before resume: because `theory_version` returns to 1, any stale value 1 could otherwise false-positive its Gate-4 equality check and silently skip Stage 2b, mechanism/design review, Stage 3a, or Stage 3b on the pivoted theory. Append to `pivot_history`. Set `pivot_resolved: null` (will be set true/false after the pivoted theory's empirical run).
2. **Pivoted theory.** Launch `theory-generator` in `pivot` strategy mode with: original theory, contradicted finding, triager report, literature map. The agent rebuilds around explaining the contradiction; original theory becomes a nested case. Save the pivoted theory as `output/stage2/theory_draft_v1.md` (under the newly incremented `theory_attempt`) — all downstream `_vN.md` artifacts (audits, novelty, implications, scorer outputs) use this fresh version numbering.
3. **Re-run Gate 2.** Theory-first: math audit (structured + freeform) on the pivoted theory. **Empirical-first: the mechanism-plausibility gate (`mechanism-auditor`)** on the pivoted mechanism — this is NOT skipped (see the Empirical-first mode note above); a PLAUSIBLE verdict re-sets `stage2_mechanism_version = theory_version`. Iterate as in Stage 2. **Measurement-first: the design gate (`experiment-reviewer` on the pivoted construct spec's measurement plan)** — an ACCEPT re-sets `stage2_design_version = theory_version`; the math audits fire later, on the pivoted run's post-Stage-3b characterization (see the Measurement-first mode note above). Iterate as in Stage 2.
4. **Re-run Gate 3.** Novelty check on the pivoted theory. KNOWN/INCREMENTAL → escalate.
5. **Re-run Stage 2b.** Theory exploration on the pivoted theory.
6. **Re-run Stage 3 (implications) IN FULL.** Derive new implications from the pivoted theory and gap-scout each one for the NOVEL/PUZZLE-CANDIDATE/SUPPORTED/DEAD tag. **Do not reuse the previous theory's implications.md** — overwrite it. Downstream agents (paper-writer, scorer, empiricist) read the current `implications.md` and assume it describes the current theory.
7. **Re-run the contradiction check on the pivoted theory.** If `--ext empirical` or `--ext theory_llm` is enabled, re-run `empirical_analysis` (and experiments, if applicable) against the new predictions and run the puzzle-triage entry check again at the end. **If neither extension is enabled (Stage-3 lit-check trigger):** the re-run reduces to the new theory's Stage 3 lit-check, with one extra requirement to prevent silent-theory false positives — the pivoted `implications.md` MUST contain at least one implication that directly addresses the originally contradicted prediction (i.e., explains why the literature shows what it shows). The orchestrator verifies this by reading the original triager report's "Contradiction" section and checking that some new implication maps onto it. Resolution rule: `pivot_resolved=true` only if (i) at least one new implication addresses the original contradiction AND (ii) no new PUZZLE-CANDIDATE appears for any new prediction. Dropping the prediction silently does not count as resolution.
8. **Set both `pivot_resolved` (top-level) AND `pivot_history[N].resolved` for this round's entry.** Set the same value to both:
   - `true` if the pivoted theory's empirics confirm its (new) predictions, or (no-empirics path) the resolution rule in step 7 is satisfied (new implication addresses the original contradiction AND no new PUZZLE-CANDIDATE appears) — the puzzle is resolved.
   - `false` if the pivoted theory's empirics also contradict and the triager (re-fired) returns HONEST-NULL or another non-PIVOT verdict, or (no-empirics path) the resolution rule fails (no new implication addresses the original contradiction, or any new PUZZLE-CANDIDATE appears).
   - If the triager returns PIVOT again, leave both fields `null` for this round and re-enter the pivot sequence (this is the second pivot; cap is 2).
9. **Proceed to Stage 4** once `pivot_resolved` is set (true or false).

## State updates

When PIVOT verdict fires, update `pipeline_state.json`:

```json
{
  "loops": { "pivot": { "round": <previous + 1> } },  // other loop entries unchanged; increment loops.pivot.round only
  "pivot_resolved": null,    // set true/false after pivoted theory's empirical run
  "pivot_history": [
    ...,
    {
      "round": <new round>,
      "contradiction": "<one sentence>",
      "prior_source": "<which paper / data source set the prior>",
      "new_mechanism_hint": "<what the triager suggested>",
      "resolved": null         // set true/false at end of pivot sequence
    }
  ],
  "triaged_lit_implications": []   // reset on PIVOT — new theory, new implication space
}
```

**Two fields — don't confuse them:**

- **Top-level `pivot_resolved`** — overall current state. Reflects the most recent pivot's outcome. This is the **single signal** downstream agents (paper-writer, scorer) read. Values:
  - `null` — no pivot has occurred yet, or the most recent pivot is still in progress
  - `true` — the latest pivoted theory's empirics confirmed its predictions; the puzzle is resolved
  - `false` — the latest pivoted theory also contradicted, ended in HONEST-NULL or escalated; the puzzle was not solved
- **Per-round `pivot_history[N].resolved`** — the resolution of pivot round N specifically. Useful for auditing history across multiple pivots. Set to the same `true`/`false` value at the same time as `pivot_resolved`; the top-level field mirrors the latest entry.

Paper-writer and scorer should gate their puzzle-framing logic on **top-level `pivot_resolved == true`**, NOT on `loops.pivot.round > 0`. A failed pivot is not a resolved puzzle.

Append to `history` array as usual:
```json
{ "timestamp": "...", "event": "puzzle-pivot round N — <new mechanism hint>" }
```

## Re-fire guard for the Stage-3 lit-check trigger

Stage 3 can re-run on RECONCILE, on Gate-4 REVISE→Stage 2 cycles, and after PIVOT. Without a guard, the same PUZZLE-CANDIDATE implication can re-fire the triager every time. Track triaged Stage-3 PUZZLE-CANDIDATEs in `pipeline_state.json`:

```json
"triaged_lit_implications": [
  { "implication_key": "<canonicalized text — see below>", "verdict": "FIX-EMPIRICS-b", "triage_file": "output/puzzle_triage/triage_pN.md" }
]
```

**Block-on-rerun rule.** Stage 3 step 5 checks this list before firing the triager. The ONLY terminal verdict that blocks re-firing is **FIX-EMPIRICS-b** (DEBATABLE lit — theory unchanged, lit-evidence too weak). All other verdicts must NOT block re-fires:
- **RECONCILE** mutates the theory and restarts Stage 3 — if the same implication reappears in the new theory it deserves a fresh triage. When RECONCILE fires, the orchestrator removes that implication's entry from `triaged_lit_implications`.
- **BACK-TO-IDEA** restarts Stage 1 with a new idea → reset `triaged_lit_implications` to `[]`.
- **HONEST-NULL** ends the run on this theory → moot (no further Stage 3 runs).
- **PIVOT** → reset `triaged_lit_implications` to `[]` (new theory, new implication space).

**Implication key (canonicalization).** Match on `lowercase + whitespace-collapsed` form of the implication's one-sentence statement. False negatives (re-firing on a slightly reworded implication) are cheap — the triager just runs again. False positives (silently blocking a different implication that happens to canonicalize the same) are expensive. When uncertain, prefer re-firing.

**Append responsibility.** The orchestrator updates `pipeline_state.json:triaged_lit_implications` after each Stage-3 triager run completes — the triager agent itself does not write to pipeline state, only to `output/puzzle_triage/triage_pN.md`. See Procedure step 4.

## Hard cap

`loops.pivot.round >= loops.pivot.cap` → triager is forbidden from recommending PIVOT. Two pivots without resolution means the problem is not tractable on this approach. Default to HONEST-NULL.

## Downstream effects

When `loops.pivot.round > 0`:
- **Paper-writer**: frames the introduction around the puzzle, not the original prediction. The original theory becomes a baseline / null; the new mechanism is the contribution.
- **Scorer**: Surprise dimension floor is 70 (resolved puzzle is by construction surprising). Importance benefits if the puzzle was field-visible.
- **Referee simulation**: should specifically probe whether the resolving mechanism is principled ({{PRINCIPLED_MECHANISM_PHRASE}}) vs. ad-hoc (curve-fit to the data). This is the main failure mode of pivot papers.
