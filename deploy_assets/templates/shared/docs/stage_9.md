# Stage 9: Polish

**Agents:** `polish-consistency`, `polish-formula`, `polish-numerics`, `polish-institutions`, `polish-equilibria`, `polish-identification`, `polish-bibliography`, `polish-prose`

This is the final substantive pass before the pipeline marks the paper complete. It runs *after* style edits (Stage 7) and *after* bibliography verification (Stage 8) — by this point the references file is clean, mechanical style is fixed, and the prose is in its final form. The eight polish agents read that final form with disjoint, focused checklists and produce structured fix lists. Together they catch the failure modes that referees flag immediately and that earlier checks systematically miss because those checks evaluated working theory/analysis sources rather than the rendered LaTeX, and because cross-section consistency, calibration sanity, institutional realism, identification coherence, and prose economy were never anyone's job.

Stage 9 hands off to Stage 10 (Lessons), which owns the `"status": "complete"` flag. The pipeline is not done until polish has either applied its fixes or judged that nothing actionable remains *and* the orchestrator has written the two lessons documents.

## Procedure

<!-- AUTONOMOUS_START -->
1. **Initialize the round.** Read `loops.polish.round` from `process_log/pipeline_state.json`. On first entry to Stage 9, increment it from 0 to 1 (so the first round writes `_r1.md` files). On a re-run after step 6 below, increment again. All polish artifacts in this round use the suffix `_r{N}.md` so prior rounds are preserved on disk.
<!-- DATA_FIRST_START -->
   **Crash-recovery preflight:** read `claim_discipline_gate` before launching any polish agent. When its phase is `repair_pending`, require its exact attempt-qualified `report_path` to contain a valid NEEDS-FIXES report, keep `current_stage = "stage_9"`, and resume step 9's binding repair subprocedure from that report regardless of the current `loops.polish.round`; do not launch a replacement semantic audit or route the cap first. When its phase is `audit_pending`, reconcile exactly the precommitted audit in step 9. A valid NEEDS-FIXES remains binding and routes normally even if later bytes made its stored receipt stale. Before accepting PASS or consuming a missing/invalid report's re-fire, however, verify the current `stage9-final` receipt with `--rerender` and require its exact SHA-256 to equal `paper_receipt_sha256`; on failure, atomically clear the obsolete audit object, keep `current_stage = "stage_9"`, and return to step 7. With a still-matching receipt, route a valid report at `report_path`; if it is absent, empty, malformed, or internally inconsistent and `launches == 1`, consume the sole re-fire by atomically precommitting a fresh next-attempt path with `launches = 2` before firing; at `launches == 2`, halt as `halted_claim_discipline`. Any other object shape or value is a corrupt durable obligation and halts. When the object is null, follow the ordinary stage procedure.
<!-- DATA_FIRST_END -->

2. **Launch all eight polish agents in parallel.** Pass each agent the current `loops.polish.round` value `{N}` in its prompt. Each reads `paper/main.tex`, the included sections, and (where relevant — the agent body specifies) the latest theory draft at `output/stage2/theory_draft_vN.md` (highest N present; glob `output/stage2/theory_draft_v*.md` and pick the highest version). **IA-aware inputs:** if `paper/internet_appendix.tex` is non-empty beyond the placeholder skeleton (paper-writer populated it), also pass `paper/internet_appendix.tex` and any `paper/sections/internet_appendix/*.tex` files to all eight polish agents. The agent bodies cover IA scope, but they will not self-discover the IA without prompt instruction. **Extension-aware inputs:** if `--ext empirical` is enabled and `pipeline_state.json:stage3a_analysis_path` is non-null, resolve its receipt exclusively from `pipeline_state.json:stage3a_result_receipt`, then pass that exact report and its bound exhibits to `polish-numerics`, `polish-consistency`, and `polish-identification`. If `--mode empirical-first` is active, tell `polish-prose` so in its prompt — this is the mode signal its item 11 (mechanism over-weight) gates on. Item 11 fires only when `paper/sections/mechanism.tex` exists, which only empirical-first produces; do **not** signal item 11 for a theory-first `--ext empirical` run (it has `model.tex`, no `mechanism.tex`, and item 11 must not fire on it). Also pass `output/stage3a/identification_menu.md` (theory-first --ext empirical) or `output/stage1/identification_design.md` (empirical-first) and `output/stage3a/identification_audit.md` to `polish-identification` — these are its primary input files. If `--ext theory_llm` is enabled and `pipeline_state.json:stage3b_results_path` is non-null, pass that exact report and the exhibits bound by `pipeline_state.json:stage3b_result_receipt` to `polish-numerics` and `polish-consistency` for the same reasons.
**Receipt resolution:** wherever this stage says the active receipt for `stage3a_analysis_path`, resolve it exclusively from `pipeline_state.json:stage3a_result_receipt`; never scan or guess among active registry entries.
<!-- EMPIRICAL_FIRST_START -->

   **Mode-aware applicability under `--mode empirical-first`:** `polish-formula` and `polish-equilibria` produce reduced or N/A reports in this mode — the paper has no theorems, no equilibria, and no FOC-based derivations to verify. Both still launch (their bodies handle the N/A case by producing a brief "no applicable content" report, which is cheaper than gating the launch at the orchestrator). The other six polishers (`polish-numerics`, `polish-institutions`, `polish-bibliography`, `polish-consistency`, `polish-prose`, `polish-identification`) are fully applicable and produce useful findings. **`polish-identification` is the primary identification-coherence auditor in this mode** — symmetric counterpart to `polish-equilibria`'s structural-model audit under theory-first; pass it `output/stage1/identification_design.md`, the exact report at `pipeline_state.json:stage3a_analysis_path`, and `output/stage3a/identification_audit.md` per its body's input list. Also pass `output/stage1/identification_design.md` to `polish-consistency` so prose claims about the design's estimand and assumptions are cross-checked against the authoritative design.
<!-- EMPIRICAL_FIRST_END -->

<!-- DATA_FIRST_START -->
   **Mode-aware applicability under `--mode data-first`:** `polish-formula` and `polish-equilibria` produce reduced or N/A reports in this mode — the paper has no theorems, no equilibria, and no FOC-based derivations to verify. Both still launch (their bodies handle the N/A case). The extension-aware instruction above to pass `identification_menu.md` / `identification_design.md` / `identification_audit.md` does **not** apply — those files do not exist in this mode (the identification agents are pruned) and their absence is by design, not a defect. The generic "highest theory draft" rule above also does **not** select the data-first spec: version numbers reset across attempts and abandoned higher-numbered files can remain. First require non-null `pipeline_state.json:dataset_spec_version == pipeline_state.json:theory_version`, then resolve the binding specification exclusively as `output/stage2/theory_draft_v{dataset_spec_version}.md`; a missing path or failed equality returns to Gate 2 and cannot enter polish. **`polish-identification` runs with its data-first body (the causal-overreach and estimand-discipline backstop) and must NOT produce an N/A report on the grounds of missing identification artifacts** — pass it that exact binding specification, the exact report at `pipeline_state.json:stage3a_analysis_path` with its bound receipt, and `output/stage3a/coverage_audit.md`. Its findings are prose-level: causal language on descriptive facts, coverage claims beyond the validation, robustness adjectives without sensitivity panels, unearned adjudications, release-boundary slips. Also pass the same exact dataset specification to `polish-consistency` so prose claims about coverage, conventions, and waivers are cross-checked against the authoritative spec, and to `polish-numerics` so headline coverage counts are recomputed against the build report.
<!-- DATA_FIRST_END -->
   Each agent writes its own report:
   - `output/polish_consistency_r{N}.md`
   - `output/polish_formula_r{N}.md`
   - `output/polish_numerics_r{N}.md`
   - `output/polish_institutions_r{N}.md`
   - `output/polish_equilibria_r{N}.md`
   - `output/polish_identification_r{N}.md`
   - `output/polish_bibliography_r{N}.md`
   - `output/polish_prose_r{N}.md`

   Run them in parallel — they have no dependencies on each other and write to disjoint output paths.

   **Write-verification gate (do this before step 3 — do not skip).** A polish agent can return a task-notification summary yet silently fail to write its `output/polish_*_r{N}.md` file; if you hand the triager only the reports that happen to exist, the missing agents' findings are dropped from the audit trail with no signal (this is a real, observed failure — an entire agent's criticals lost from triage). After the parallel batch returns, verify that **every polish agent you launched this round produced its expected `output/polish_*_r{N}.md` file and that the file is non-empty** (a brief "no applicable content" report under `--mode empirical-first` counts as written; a zero-byte or absent file does not). For each agent whose file is missing or empty, **re-fire that specific agent once** with the same prompt. If it is still missing after the single re-fire, do **not** proceed silently: record the agent name and the missing path explicitly at the top of the triager's input (a `## Missing polish reports` note naming each absent agent) so the gap is visible in `output/polish_triage_r{N}.md`, and carry the same note into the round's `## Known limitations` if it survives to Stage 10. Never let a polish agent's absence be indistinguishable from a clean report. Commit nothing for this gate; it is a pre-triage integrity check.

3. **Aggregate via `triager`** (Stage 9 context — see the triager body's "Stage 9 output format" section). Launch the `triager` agent with the eight polish report paths as input and instruct it to write `output/polish_triage_r{N}.md`. The triager classifies each finding into three buckets per its Stage 9 rules (4, 5, and 6):
   - **Apply** — concrete fixes the agents identified with sufficient evidence (verbatim quote + suggested fix); criticals default to Apply, polish-formula criticals always Apply (rule 4 override), same-anchor findings across agents are deduplicated, and polish-prose cuts that conflict with another agent's keep/fix at the same anchor are dropped (rule 6).
   - **Investigate** — major-severity findings that lack a one-token fix and need paper-writer judgment, or criticals downgraded from Apply with a concrete justified conflict.
   - **Drop** — minor-severity findings that aren't one-token edits, plus any item the triager justifies dropping. Each drop has a one-line written justification.

   Before paper-writer passes, route any finding whose fix changes a computed value, result-bearing table cell, or plotted series to its producer. The producer must rerun its stage analysis/bundle/renderer and substantive review; paper-writer may then update only the surrounding prose/caption. Paper-writer never hand-fixes computed exhibits.

4. **Re-launch `paper-writer` in two sequential passes (not one).** Applying every triaged fix — formula corrections, numeric fixes, consistency rephrasings, structural relocations, *and* prose cuts — in a single invocation overloads one context and produces shallow edits on a long list; and a fresh prose-tightening pass should see the paper's *settled* structure, not edit prose that a later relocation is about to move. So split the already-deconflicted Apply/Investigate list across two **sequential** (never parallel — they edit the same `paper/sections/*.tex` files; concurrent writes race) `paper-writer` instances. In each prompt pass the *resolved* path with `{N}` substituted to the current round integer (e.g., `output/polish_triage_r1.md`, not the variable form), and tell the instance which pass it is and which rows it owns. Paper-writer's body has a "When re-invoked at Stage 9" section that handles Apply / Investigate rows.

   - **Pass 1 — structure + correctness.** This instance applies every row *except* polish-prose subtractive findings: all correction rows (polish-formula, polish-numerics, polish-consistency, polish-institutions, polish-equilibria, polish-identification, polish-bibliography) **and** the polish-prose additive/structural rows (`Disp. = add`/`rebalance` — items 10–13, including item-13 robustness relocations to the appendix/IA). Structure settles first so prose tightening in pass 2 reads the final layout. Instruct it: "Stage 9 pass 1 of 2: apply every Apply row whose `Disp.` is blank, `add`, or `rebalance`; skip every polish-prose row whose `Disp.` is `cut`, `compress`, or `rewrite` (pass 2 owns those). Run the cut-scan pre-processing pass only over rows you own."
   - **Pass 2 — prose, on the settled paper.** A second fresh instance applies the polish-prose subtractive findings (`Disp. = cut`/`compress`/`rewrite` — items 1–9, the whole subtractive family) against the structure pass 1 left. This matches polish-prose's existing subordination to every other agent. Instruct it: "Stage 9 pass 2 of 2: apply only the polish-prose Apply rows whose `Disp.` is `cut`, `compress`, or `rewrite`; the structural and correction fixes are already applied — read the current `paper/sections/*.tex`. Run the cut-scan pre-processing pass over your `cut`/`compress` rows first." Skip pass 2 entirely if no such rows exist.

   Each instance edits `paper/sections/*.tex` (and, for item-13 relocations, `paper/internet_appendix.tex`) to incorporate its Apply rows, drafts candidates for any Investigate rows it owns, and appends to the `## Investigate decisions` section of the triage file with one sentence per draft. After **each** pass returns and its Investigate decisions are accepted/revised/reverted, run `docs/results_evidence.md` with checkpoint `stage9-rN-p1` or `stage9-rN-p2` before launching the next mutation. Commit once after both passes: `paper: stage 9 polish — round r{N} fixes applied`.

   If paper-writer's Stage 9 fixes added or removed citations (regardless of which polish agent flagged them), re-launch `bib-verifier` once on the new state to confirm. This is a one-shot confirmation, not a return to Stage 8. **Do not increment `loops.bib_verify.round` for this call** — it is a post-polish sanity check, not a Stage 8 loop iteration. The Stage 8 cap (`loops.bib_verify.round >= loops.bib_verify.cap`) does not apply.

5. **Re-run `style` once on the post-polish prose.** Polish edits — especially polish-prose consolidations and polish-consistency rephrasings — are prose-shaped, so style violations may have been reintroduced into prose that was style-clean after Stage 7. Launch `style` with the same prompt shape it received at Stage 7; it will edit `paper/sections/*.tex` directly for mechanical fixes and append judgment calls to `paper/style_report.md`. Then run `docs/results_evidence.md` with checkpoint `stage9-rN-style` and commit the style edit with its bound PASS. Skip this step only if neither paper-writer pass in step 4 reported any edits.

6. **Re-run polish if there were any Apply-bucket criticals.** If `output/polish_triage_r{N}.md` contained ≥1 row in the `Apply` table tagged `critical`, re-enter Stage 9 from step 1 (increments `loops.polish.round` to N+1). At step 2 of the new round, launch *only the agents whose criticals were applied* — the triage file's "Re-run trigger" line in the summary names them. **Hard cap: at most 2 rounds total (N=1, N=2).** When the round-2 cycle finishes (paper-writer has applied round-2 fixes and style has re-passed), proceed to step 7 regardless of whether criticals remain. If the round-2 triage still contained Apply criticals, ship the partial fix and note the unresolved findings under a "Known limitations" paragraph in `paper/sections/discussion.tex` before Stage 10 marks the pipeline complete.

7. **Rebuild and re-run the rendered-table gate on the final paper.** Stage 7 style edits, Stage 8 citation repairs, and both Stage 9 paper-writer passes occurred after the first Stage-5 render audit, so that earlier PASS is stale. Run the complete Stage-5 step-7 build recipe and all six checks on the current `paper/main.tex` (plus the Internet Appendix sequence when populated), then launch `table-auditor` exactly as in Stage 5 step 8 and overwrite `output/stage5/table_legibility.md` with the final-render report.
   - **PASS:** reset `loops.table_legibility.round` to `0` and continue to step 8.
   - **REVISE:** increment `loops.table_legibility.round`, re-launch `paper-writer` with only layout findings that do not change producer-rendered numerical content (route any exhibit-content change to the producer), re-run `style` once if the repair changed prose/caption/note, run `docs/results_evidence.md` with checkpoint `stage9-tables-rN`, and repeat this step from the build. The repair rewrite does not reset the loop. At cap, halt for operator routing.
   - **GATE-BROKEN**, a missing/empty report after one write-integrity re-fire, or an undeclared verdict: halt exactly as Stage 5 specifies. A final PDF that was not rendered and inspected cannot advance.

8. **Run final evidence and prepare the Stage 10 handoff.** When polish stabilizes and the final rendered-table gate passed, run one mandatory final `docs/results_evidence.md` checkpoint `stage9-final`, then verify its bound paper receipt with `--rerender`. Only that PASS, plus any subsequent mode-specific gate below, may update `current_stage` to `stage_10` and proceed. Do not set `"status": "complete"` here — Stage 10 owns that flag. Commit the stabilized Stage 9 boundary only when every applicable gate has passed: `paper: stage 9 polish — round r{N} stabilized`.

<!-- DATA_FIRST_START -->
   Under `--mode data-first`, a REVISE at this final evidence checkpoint can cause producer, exhibit, citation, caption, or prose mutations. Complete the evidence repair loop, but do not treat its eventual PASS as the handoff: every such mutation invalidates the settled render and claim verdict. Return to step 7 for a fresh rendered-table PASS, repeat this `stage9-final` checkpoint on those bytes, and then continue to step 9. Only a `stage9-final` PASS followed by step 9 PASS may update `current_stage`.

9. **Run the data-first final claim-discipline gate.** The ordinary `polish-identification` report was written before the Stage 9 paper-writer, style, bibliography, rendered-table, and final-evidence repair mutations, so it cannot certify the manuscript that will ship. With `claim_discipline_gate = null`, choose fresh attempt `A` as one plus the highest integer in any existing `output/polish_identification_final_r{N}_a*.md` sibling (or 1 when none exists); never delete or overwrite those reports. Compute the exact lowercase SHA-256 of the currently passing `process_log/paper_evidence.receipt.json`, then atomically set `claim_discipline_gate = {"phase":"audit_pending","report_path":"output/polish_identification_final_r{N}_a{A}.md","paper_receipt_sha256":"sha256:<64 lowercase hex>","polish_round":N,"launches":1}`, append the launch permit to history, and commit **before** the physical agent launch. Launch the existing `polish-identification` agent on the exact manuscript bytes bound by that receipt, with the same binding dataset specification, exact active Stage 3a report and receipt, and `output/stage3a/coverage_audit.md` inputs required in step 2. Instruct it that this is the final claim-discipline gate and that this launch must freshly write the object's exact `report_path`. The agent may not write any other final path.

   Reconcile only the precommitted `report_path`. Verify it exists, is non-empty, and begins with exactly one first-line routing field: `VERDICT: PASS` or `VERDICT: NEEDS-FIXES`. Also require internal consistency: PASS is valid only when `## Findings` contains zero finding rows and `## Quick verdict` reports zero critical, major, and minor findings; any finding requires NEEDS-FIXES, and NEEDS-FIXES with no finding is malformed. A summary without that artifact or an invalid report consumes the sole re-fire only as the step-1 preflight specifies: allocate the next unused attempt path, atomically replace `report_path` and set `launches = 2` before the second physical launch, keeping the same receipt digest and round. A second failure halts. Never launch first and register its path afterward.

   - **PASS:** require `claim_discipline_gate.phase = "audit_pending"`, the object's report path to equal this report, and its stored receipt digest to equal the freshly recomputed digest of the still-PASS `stage9-final` receipt; then verify that receipt once more with `--rerender`. Only those checks plus this verdict may atomically set `claim_discipline_gate = null`, update `current_stage` to `stage_10`, and proceed. If the digest differs or verification is stale/non-PASS, atomically clear the obsolete audit object while keeping `current_stage = "stage_9"`, return to step 7, and repeat the rendered-table, `stage9-final`, and final claim gates on one settled manuscript. Include the final claim report, receipt verification, cleared object, and state transition in step 8's stabilized-boundary commit.
   - **NEEDS-FIXES while `loops.polish.round < loops.polish.cap`:** before editing the paper, atomically change `claim_discipline_gate.phase` from `audit_pending` to `repair_pending`, increment `loops.polish.round` by one, append the repair obligation to history, and commit that state plus the already-precommitted report. Then re-launch `paper-writer` with only that exact report and instruct it to reconcile every finding against the current manuscript, applying only findings not already resolved. It must narrow or restate unsupported claims, place a required caveat at most once, or expose already-produced validation material; it must not invent identification, new evidence, hand-edit computed exhibits, or duplicate language during crash replay. If the repair changes citations, run the one-shot `bib-verifier` confirmation from step 4; re-run `style`; then run `docs/results_evidence.md` with checkpoint `stage9-claim-discipline-r{N}` using the incremented round. After that checkpoint passes, atomically set `claim_discipline_gate = null`, append the completed repair to history, and commit while leaving `current_stage = "stage_9"`; then return to step 7. The repaired manuscript must earn a new rendered-table PASS, `stage9-final` evidence PASS, and final claim-discipline PASS in that order. On any crash while phase is `repair_pending`, step 1 resumes this repair before any new audit.
   - **NEEDS-FIXES at `loops.polish.cap`:** retain the exact `audit_pending` object, atomically set `status = "halted_claim_discipline"`, leave `current_stage = "stage_9"`, commit the state and final report, and stop for operator review. This mode does not use step 6's generic "ship the partial fix with known limitations" escape for unresolved claim-discipline findings.

   This is a hard routing gate on the semantic auditor's verdict, not a claim that causal meaning can be recognized mechanically. Do not substitute a causal-verb string scan: attribution, prediction, and unsupported causal interpretation require context.
<!-- DATA_FIRST_END -->

<!-- AUTONOMOUS_END -->
<!-- MANUAL_START -->
Manual deployments use the canonical manual sequence under **Notes → Manual-mode chaining** below. Read the round from `process_log/manual_evidence_state.json`, resolve computed sources exclusively through active entries in `process_log/results_registry.json`, and never create or infer autonomous stage pointers.
<!-- MANUAL_END -->

## What each polish agent owns

| Agent | Owns | Does not own |
|---|---|---|
| polish-consistency | Cross-section contradictions, label↔object mismatches, headings vs. text, intro vs. later qualifications | Formula correctness; numerical reproduction |
| polish-formula | Re-derivation of every numbered equation; sign / subscript / abs-value errors paper-writer introduced | Numerical examples; whether prose contradicts the formula |
| polish-numerics | Recomputing every numerical claim; stock-vs-flow; normalized-vs-unnormalized; baseline IR feasibility | Formula correctness; institutional realism of the calibration |
| polish-institutions | Real-world facts (regulatory mechanisms, fee conventions, market sizes); faithful characterization of cited papers | Cite-key validity (bib-verifier); equation correctness |
| polish-equilibria | Multiple equilibria in fixed-point regions; LLN/continuum assumptions; reduced-form↔structural bridges; benchmark choice | Anything mathematically wrong with stated equilibria |
| polish-identification | Estimand-vs-claim alignment; diagnostics-vs-design coverage; cluster level vs. design level; identification.tex faithfulness; heterogeneity-population coherence; robustness vs. named failure modes; out-of-scope structural claims | Code correctness (empirics-auditor); design plan decisions (identification-auditor); theory mechanisms (polish-equilibria) |
| polish-bibliography | Per-citation prose-claim verification (FAITHFUL/APPROXIMATE/MISCHARACTERIZED/DECORATIVE) | Cite-key existence (bib-verifier) |
| polish-prose | **Subtractive:** repeated caveats, hedge stacking, abstract bloat, undefined acronyms, defensive framing, buried thesis sentences, section-opener resummary. **Additive/structural (items 10–13):** under-motivated intro (missing stakes / literature anchor), mechanism over-weight in empirical papers, broken through-line, mis-weighted caveat, over-stacked main-text robustness (relocate to appendix/IA) | Mechanical style (style agent); contradictions (polish-consistency); whether a caveat is correct |

The deliberate overlap between `polish-institutions` and `polish-bibliography` on egregious citation mischaracterization is fine — both will surface the same critical, the triager will dedupe by anchor. The deliberate subordination of `polish-prose` to every other polish agent (rule 5 precedence + rule 6 removal-vs-fix) is also fine — a prose *cut* that conflicts with another agent's keep/fix loses, because deletion eliminates the substrate the other agent was editing. **This subordination governs polish-prose's *subtractive* findings only.** Its additive/structural findings (items 10–13 — motivation, mechanism rebalancing, through-line bridge, caveat demotion, robustness relocation) carry `Disposition: add` or `rebalance` and are *not* cuts, so triager rule 6 (which fires only on a polish-prose *cut* at a shared anchor) does not apply to them; they route to `Apply`/`Investigate` on their own severity like any other agent's findings, and only the same-anchor *fix-vs-fix* precedence of rule 5 can demote them. Paper-writer applies them as small additions/rebalancing per its "Additive / structural polish-prose findings" note, not as deletions.

## Notes

- **Manual-mode chaining.** When a user runs polish manually (not via the autonomous orchestrator) they are their own orchestrator: pass `{N}=1` to all eight agents on the first chained run; if anything is re-run after paper-writer applies fixes, pass `{N}=2`. Agent bodies default to `N=1` when no value is supplied, so the round-1 path works out-of-the-box. Use this canonical manual sequence (round 1); every mutation checkpoint follows `docs/results_evidence.md` and reads its counter from `process_log/manual_evidence_state.json`:
  1. **Parallel launch:** `polish-consistency`, `polish-formula`, `polish-numerics`, `polish-institutions`, `polish-equilibria`, `polish-identification`, `polish-bibliography`, `polish-prose` — invoke all eight in one batch. Tell them to read `paper/main.tex`, its sections, and the IA when populated. For computed projects, enumerate `process_log/results_registry.json:active`; pass the exact reports, artifacts, and exhibits bound by those active receipts to the relevant numerics/consistency/identification agents. Manual mode has no `pipeline_state.json` result pointers and must not guess them from filenames. Pass identification menu/audit files when they exist.
  2. **Triager:** invoke `triager` with the prompt "Stage 9 context, loops.polish.round = 1; inputs are the eight output/polish_*_r1.md files; write output/polish_triage_r1.md".
  3. **Paper-writer (two sequential passes):** invoke `paper-writer` with "Stage 9 polish round, pass 1 of 2: read output/polish_triage_r1.md and apply every Apply row whose `Disp.` is blank, `add`, or `rebalance` (structure + correction fixes, incl. item-13 robustness relocations); skip polish-prose `cut`/`compress`/`rewrite` rows". Read the appended `## Investigate decisions` and accept/revert, then complete evidence checkpoint `manual-stage9-r1-p1`. Invoke a fresh `paper-writer` with "Stage 9 polish round, pass 2 of 2: apply only the polish-prose Apply rows whose `Disp.` is `cut`, `compress`, or `rewrite`, against the current section files"; skip pass 2 if there are no such rows. Read its decisions and, when it edits, complete checkpoint `manual-stage9-r1-p2` before continuing.
  4. **Style re-pass:** invoke `style` with the same prompt shape it used at Stage 7. Skip only if *neither* paper-writer pass in step 3 reported any edits. If style edits, complete checkpoint `manual-stage9-r1-style`.

  If the round-1 triage contained Apply criticals, repeat with `loops.polish.round = 2` (only the agents whose criticals were applied) and use the analogous `manual-stage9-r2-*` checkpoints; stop after round 2. Then rebuild the PDFs and invoke `table-auditor` on the final render before treating the manual chain as complete; any layout repair gets its own checkpoint. Finish with mandatory checkpoint `manual-stage9-final` and verify its paper receipt with `--rerender`. A prior Stage-5 PASS predates these edits and is stale.
- **Why polish is last, not earlier.** Style (Stage 7) sometimes silently changes a sentence that would have been a polish-consistency finding; bibliography verification (Stage 8) sometimes drops or reanchors cites that would have been polish-bibliography findings. Running polish *after* both means the agents are reading the final form of the paper. Style is then re-run at the end of Stage 9 (step 5) on the post-polish prose so mechanical violations reintroduced by polish edits don't ship. The cost is one wall-clock pass plus one fast style call per polish round.
- **Why a separate stage rather than another referee round?** Referees evaluate the paper's contribution and mechanism; they do not re-derive every equation, recompute every number, or look up every cited paper. The polish agents have narrow, mechanical-but-substantive checklists that referees skip. Running both gets coverage of both failure modes.
- **Why eight agents instead of one?** A single "find everything wrong with this paper" prompt produces shallow coverage of every category. Disjoint, focused prompts produce deeper coverage of each. The cost of eight parallel calls is one wall-clock pass.
- **What if a polish agent contradicts a referee request?** The triager judges. A referee request that survived Stage 6 has higher priority by default — drop the polish finding with a justification. Exception: a polish-formula critical (provably wrong equation) overrides any referee request, because the equation is wrong regardless of what the referee wanted.
- **Polish does not loop with referees.** If the polish agents surfaced a new fundamental issue (rare), commit the partial fix and flag it as a known limitation in the paper's discussion section. Do not reopen Stage 6.
- **Post-pipeline math-audit rule still applies.** Polish runs *inside* the pipeline. After Stage 10 marks `"status": "complete"`, any further proposition/lemma/corollary edits go through the post-pipeline math-audit procedure documented in `core.md`.
