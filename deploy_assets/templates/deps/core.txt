# Core Python deps for the pipeline (all variants).
# Single source of truth: read by setup.sh via `uv pip install -r`.
# This source-tree file is a build input. Setup also copies it into the
# manifest-owned `.arpipeline/update_inputs/deps/` provisioning record inside
# each deployment; that deployed copy is not a project-authored dependency file.
sympy
matplotlib
certifi
